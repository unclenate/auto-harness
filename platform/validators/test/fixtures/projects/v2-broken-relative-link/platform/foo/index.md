# v2 Broken Relative Link Fixture

This file contains a sibling-dir relative link that does not resolve on disk.
The v1 validator missed this class entirely (no `platform/` prefix).

Broken relative parent-dir link: [missing](../bar/does-not-exist.md).
