# This repo IS the harness platform — its docs, skills, and validators legitimately
# discuss the [[PLACEHOLDER_NAME]] and YYYY-MM-DD conventions they enforce.
# Exclude platform infrastructure from self-scan; consumer projects won't need this.
platform/**
legacy/**
README.md
# CONTRIBUTING.md explains the placeholder convention to outside contributors.
CONTRIBUTING.md
# Implementation plans legitimately contain template content showing
# file bodies to be created — the [[PLACEHOLDER_NAME]] tokens are illustrative.
docs/superpowers/plans/**
# Design specs (brainstorming output) are the sibling of plans: they show
# template bodies and discuss the [[PLACEHOLDER_NAME]] convention as spec text,
# not as unfilled tokens. Same rationale as the plans exemption above.
docs/superpowers/specs/**
# PRD-0005 specifies the token names a future implementation will introduce
# into templates — token mentions in this PRD are spec text, not unfilled
# tokens. Same rationale as the platform/** exemption above. Re-evaluate
# this exemption when PRD-0005 acceptance criteria are met (the PRD's
# future-work section captures teaching the validator to skip code spans,
# which would obviate this exemption).
docs/requirements/PRD-0005-consumer-header-hygiene.md
# The 2026-05-27 documentation-audit working folder is reference material that
# discusses the YYYY-MM-DD and [[PLACEHOLDER]] conventions as audit prose (e.g.
# tables describing what validate-placeholders enforces), not unfilled tokens in
# shipped docs. Excluding it removes the long-standing need to stash the folder
# out of the tree before every validator run. It is cited as a context source by
# ADR-0016/ADR-0017; committing the folder itself is tracked as a separate
# follow-up.
documentation-audit-2026-05-27/**
