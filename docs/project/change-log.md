<!--
Copyright 2026 Nate DiNiro <UncleNate@gmail.com>
SPDX-License-Identifier: MIT OR Apache-2.0
Part of auto-harness — see LICENSE-MIT and LICENSE-APACHE at repository root.
-->

# Project Change Log

This log records material changes to project scope, plan, timeline, or technical direction.
It is not a git commit log — it captures *decisions and their rationale*, not code diffs.

---

## Log

| Date | Type | Change | Reason | Owner | ADR/PRD |
| ---- | ---- | ------ | ------ | ----- | ------- |
| 2026-05-27 | Fix | Excluded `docs/superpowers/plans/**` and `docs/superpowers/specs/**` from the markdownlint scan (`.markdownlint-cli2.jsonc` `ignores`). These are informal session working artifacts (implementation plans, design specs that quote template bodies), not published docs held to the doc-quality bar; they are already excluded from the placeholder scan via `.placeholder-ignore` (specs added in commit 2fd8bda). This mirrors that policy for markdownlint so the two exclusion surfaces stay aligned. Unblocks main, which went red when the managed-fleet G1 plan file (`docs/superpowers/plans/2026-05-25-managed-fleet-delivery-posture.md`) was committed with 22 markdownlint violations. | Routine CI-config alignment; cheap satisfier per operating-principle § 2. The placeholder scan already treats these dirs as informal working-doc surfaces; markdownlint should too, or every committed plan/spec risks turning main red on incidental formatting. | @unclenate | n/a |
| 2026-05-26 | Governance | Discipline-codification: promoted the **deferred-implementations** discipline to a new [operating-principles § 9 *Split Design from Implementation*](../operating-principles.md#9-split-design-from-implementation), citing its three same-session firings (PRD-0011 Option-B rejection / PRD-0013 per-rule machinery deferral / PRD-0014 trace-contract-rule deferral). Added a matching *Implementation Deferral* section to the PRD template (`platform/templates/product/prd.md`) prompting authors to enumerate deferred implementations, their follow-up, and why. Documented the **candidate-stub-with-promotion-criterion** technique as guidance in the opportunity README *template* (`platform/templates/opportunity/README.md`) — deliberately **not** promoted to an operating-principle, since it has only one end-to-end firing (OPP-0032) and its own observation gates operating-principle status on a *second* firing; documenting it now in the README template (capture how-to is policy) rather than candidates.md (organizational index only) follows the ADR-0012 / § 7 org-vs-policy split. Appended dated update notes to the three forecasting observations in `shared-observations.md` so the knowledge surface reflects what landed. | Codifies the two patterns the session itself produced. The deferred-implementations discipline cleared its three-instance bar (high confidence) and was promoted; the candidate-stub pattern had not (one firing) and got lighter-weight template documentation instead — promoting it now would itself violate the candidate-stub discipline it describes. Corrects the earlier loose claim that both patterns had three firings each: only deferred-implementations did. | @unclenate | operating-principles § 9 |
| 2026-05-26 | Scope | Promoted [OPP-0029](../opportunities/OPP-0029-agent-observability.md) `proposed` → `exploring` same-day per the established maintainer-priority cadence (OPP-0004/0005/0006/0007/0026/0032 pattern; sixth same-day OPP→exploring promotion this session). OPP-0029 is the first satellite of the Tula second-pass cluster anchor (OPP-0027) to enter PRD-pass, per OPP-0027's recommended sequencing: *"OPP-0029 (observability) — most concrete, fewest open questions, immediate Tula utility."* Drafted [PRD-0014 — Agent Observability with OpenTelemetry Semantic Conventions](../requirements/PRD-0014-agent-observability.md) as the paired design committing to a **workflow-doc + trace-contract-template v1 scope**: ship the new `architectures/agent-observability` module declaring two required artifacts (`docs/observability/trace-contract.md`, `docs/observability/exporters.md`), the matching templates under `platform/templates/observability/`, and the catalog-counts assertion bumps. **Defer companion-rule machinery** (the trace-contract-update rule + the v2 `validate-trace-contract.sh` validator) to a follow-up OPP/PRD pair — per the *deferred-implementations* discipline named in PRD-0013's paired observation. PRD-0014 covers 11 must-have FRs (module creation; two templates; SUMMARY/docs README/templates README index updates; harness-onboarding + harness-governance SKILL row updates; catalog-counts assertion bumps for modules_profiles +1, modules_all +1, templates +2; self-dogfood validator green; cross-reference network to the upstream Microsoft + Cisco Outshift multi-agent OpenTelemetry semantic conventions) plus 3 should-have FRs (Mermaid trace-flow diagram for Phase 3 of ADR-0013; one-line SKILL references; optional `recommendedSkills` placeholder for a future `harness-observability` skill). PRD takes positions on six open questions and explicitly rejects (1) implementing the trace-contract-update companion rule in v1; (2) implementing `validate-trace-contract.sh` in v1; (3) picking a specific exporter backend (the template names supported backends; the choice is consumer-project); (4) generating instrumentation code; (5) forcing agents to emit traces (module is opt-in); (6) promoting the rationale-expansion-without-rule-change discipline to operating-principles § 9 in this PRD (separate discipline-codification PR). `candidates.md` OPP-0029 row bumped to `(exploring; PRD-0014 in flight, drafted same-day)`. SUMMARY.md and docs/README.md PRD tables extended. OPP-0029 Disposition populated; status flipped exploring. Implementation deferred to a follow-up PR. **Paired observation** appended to `shared-observations.md`: *"Third instance of deferred-implementations discipline confirms the pattern is generalizable"* — the pattern fired three times this session (PRD-0011 / PRD-0013 / PRD-0014), lifting to high-confidence operating-principle candidate. The observation names two ripe follow-up moves: (a) § 9 promotion of the discipline; (b) PRD template "Implementation Deferral" section addition. Both were previously deferred awaiting a third instance; both gates now clear. | Same-day OPP→exploring+PRD draft per the established cadence. The PRD inherits the deferred-implementations discipline named twice already (PRD-0011 Option B rejection; PRD-0013 per-rule machinery deferral) and applies it cleanly to a *new contract surface* — establishing the third instance the pattern needed to lift to high-confidence evidence. The discipline lets v1 ship clean (no companion-rule, no validator) while reserving the enforcement work for v2 — a per-rule PRD that can validate the rule against real consumer adoption rather than racing to ship the rule alongside the contract. The discipline's third firing is itself a load-bearing event: two patterns this session (deferred-implementations *and* candidate-stub-with-promotion-criterion) have now observed three firings each across different topical domains, making both ripe for operating-principles § 9 promotion in a single follow-up discipline-codification PR. This row satisfies the kernel + opportunity-capture + knowledge-capture audit-trail floors tripped by the OPP-0029 status flip + PRD-0014 file creation + shared-observations.md edit. | @unclenate | PRD-0014, OPP-0029 |
| 2026-05-25 | Scope | Added `delivery/managed-fleet` posture: a fifth delivery overlay for teams that operate configuration managing a live host fleet (production blast radius, no external app/distributable). Requires `docs/ops/{fleet-inventory,change-control,config-rollback}.md`; conflicts only with `prototype`; companion rule fires on fleet-topology changes (inventory host files, `host_vars/`). Ships three `templates/ops/` templates and the overlay README. Propagated across README.md, SUMMARY.md, discovery-to-composition.md, harness-onboarding SKILL.md, and templates/README.md. NOT added to HARNESS.md Active Modules (auto-harness does not self-adopt it). Same pass fixes pre-existing drift: SKILL.md internal-platform required-artifacts and README.md missing self-hosted-oss in the delivery row. | Brownfield onboarding of `fortify-ansible/ansible-internal` surfaced a posture gap: an IaC fleet repo fits none of prototype/production-saas/internal-platform/self-hosted-oss. managed-fleet names the shape and governs its operational risks (uncontrolled apply to live hosts, undocumented topology, no rollback path). Maintainer-authorized during onboarding; integrated to `main` via rebase. | @unclenate | ADR-0015 |
| 2026-05-25 | Documentation | **Phase 2 of ADR-0013 shipped — vocabulary + trust-model + doctrine rationale.** Seven kernel/reference files substantively expanded to close the audit findings the 2026-05-24 documentation audit named under M-a (core vocabulary undefined at point of contact), M-c (glossary gaps — overlay, Bootstrap Complete, Harness Ready, lite manifest, install.sh), M-d (doctrine docs as bare bullet lists; rules without rationale), and H-a (trust-model.md is a 26-line spec for the centerpiece concept). **(1) `platform/reference/glossary.md`** gained six new definitions: *Overlay* (under Core Concepts), *Kernel* (under Core Concepts), and a new **Lifecycle and Bootstrap** section covering *install.sh*, *Lite Manifest*, *Bootstrap Complete*, and *Harness Ready*. Closes M-c entirely. **(2) `platform/core/registry/module-types.md`** gained a Core Concepts block at the top (module / manifest / composition / overlay / kernel / trust tier — the five-definition block the ADR's Phase 2 § 1 explicitly named) and an Anatomy-of-a-Module worked example (the audit ADR's Phase 2 § 6) walking through `architectures/web-app/module.yaml` line-by-line. Closes M-a's "Core Concepts block" and "anatomy" gaps. **(3) `platform/core/kernel/base/trust-model.md`** expanded from 26 lines to ~150 lines: kept the six-tier table verbatim, added an Introduction explaining the model's role + a "Why Six Tiers (and Not Three, or Twelve)" rationale section + a "What the Model Encodes" section (blast-radius principle, two-human-witness threshold at tier 5, per-session authorization, cross-runtime invariance) + an honest "Enforcement Today: Honor Code" section citing OPP-0006 / PRD-0006 for the machine-checkable enforcement work in flight. Closes H-a. **(4-7) The four kernel doctrine docs** — `doctrine.md`, `audit-model.md`, `enforcement-model.md`, `lifecycle-controls.md` — each expanded from bare bullet lists (~25 lines each) to substantive rationale-bearing docs (~80-130 lines each). Every original rule preserved verbatim at the top of each section; rationale prose added below explaining *why the rule exists*, *what failure mode it prevents*, *what alternative was rejected*. The four together close M-d. None of these doctrine docs changed any *rule* — the change class is **rationale-expansion-without-rule-change**, an audience-fit-class fix, not a contract change. Paired observation appended to `shared-observations.md`: *"Doctrine documents expanded with rationale prose — why six, why this boundary, why these stages — without changing any rule"* — names the three-way audit-finding-class taxonomy this session has now exercised (Phase 1 README rebuild = *reorder*; Phase 2 doctrine = *rationale-add*; OPP-0026/PRD-0011 sunset = *rule-change with full ADR machinery*). | Phase 2 was scoped per ADR-0013 § 5 (Phase 2 — Make the mental model click, ~2-3 days). The audit's M-a / M-c / M-d / H-a findings all share a common shape — readers can't tell *why* a rule matters from the rule alone — and the fix is the same shape across all four: keep the rule verbatim, add the rationale. The discipline named in the paired observation (don't change rules when the finding is audience-fit) is the key to landing the six-file expansion as one coherent PR rather than four separate ones: because no rule changes, no companion-rule machinery fires beyond the standard doc-edit audit-trail floor; because the expansion is purely additive, the existing operating-principle § 7 (Align File Boundaries with Change-Class Boundaries) is satisfied (one change-class, one PR). Phase 3 (visual program — embed diagrams #4..#11 into the concept docs that explain them; extend `validate-catalog-counts.sh` to cover Mermaid labels and per M-j list-completeness) and Phase 4 (navigation & catalog hygiene — module README standardization, examples-README refresh, governance-doc banners) remain queued. The Phase 2 work composes naturally with the OPP-0027..0031 cluster's PRD passes: those PRDs will reference the trust-tier model, kernel doctrine, and enforcement categories that Phase 2 makes legible to first-time readers. This row satisfies the kernel + knowledge-capture audit-trail floors tripped by the kernel/glossary/module-types edits. | @unclenate | ADR-0013 |
| 2026-05-25 | Governance | **OPP-0023 accepted + implemented (PRD-0012) — `validate-doc-references` is now consumer-aware.** Removed the `<root>/platform`-must-exist guard from `validate-doc-references.sh`: Pass 1 (the platform/ bare-path dogfood) no-ops via an empty glob when absent, Pass 2 scans the consumer's own `*.md` regardless, so a submodule consumer (platform at `.harness/platform/`) validates its own docs. Exit `2` is now reserved for a missing `<project-root>`; "nothing to scan" is a clean exit `0`. Replaced `test_missing_platform_dir_aborts` (which encoded the buggy exit-2-on-missing-platform contract) with four tests — `test_missing_project_root_aborts` (exit 2), `test_empty_dir_has_nothing_to_scan_and_passes` (exit 0), and two new no-`platform/` consumer fixtures (`consumer-no-platform-valid` → 0, `consumer-no-platform-broken` → 1); the dogfood test is unchanged. Aligned the consumer CI surface: added `validate-doc-references` to `ci-integration.md`'s validators table + minimal workflow (matching `templates/ci/github-actions.yml`, which already included it). Hardened the ripgrep install (`apt-get update &&`) in `templates/ci/github-actions.yml` + the four `ci-integration.md` example workflows (gitlab-ci.yml already ran update). Promoted OPP-0023 proposed → accepted; SUMMARY + docs/README PRD lists gain PRD-0012; one paired observation appended. Full self-test suite green (76 runs) incl. the new fixtures; shellcheck clean; the harness's own doc-references run still green. | Maintainer-directed: draft PRD + implement in one PR. The fix was small because the Ruby was already consumer-safe — only the bash guard forced the failure — confirming OPP-0023's diagnosis. Filed as PRD-0012 (not a bare implementation PR) to keep the OPP→PRD→implement trail and record the resolved design questions (consumer-aware vs remove-from-template; exit-code contract; the no-`platform/` self-test the dogfood masked). apt-get hardening bundled because FR-003 already edits the same CI files. Ships as a v0.5.3 validator-reliability patch. This row satisfies the opportunity-capture + knowledge-capture audit-trail floors tripped by the OPP-0023 acceptance edit; the paired observation satisfies the distillation rule. | @unclenate | PRD-0012, OPP-0023 |
| 2026-05-25 | Scope | Promoted [OPP-0032](../opportunities/OPP-0032-session-cycle-orchestration.md) `proposed` → `exploring` same-day per the established maintainer-priority cadence (OPP-0004/0005/0006/0007/0026 pattern; now five+ instances of same-day OPP→exploring this session). Drafted [PRD-0013 — Session-Cycle Orchestration and Review-Trigger Taxonomy](../requirements/PRD-0013-session-cycle-orchestration.md) as the paired design, committing to a **workflow-doc-only v1 scope**. The PRD covers 9 must-have FRs: (FR-001) create `platform/workflow/session-shape.md` as a new peer to `cycle-end-distillation.md`; (FR-002) document the session-boundary checkpoint taxonomy (session start / work / PR open / PR merge / post-merge / periodic-review / external-event-driven); (FR-003) document the trigger-class taxonomy (PR-boundary / session-boundary / time-boundary / count-boundary / audit-boundary / external-event-driven); (FR-004) audit currently-declared-but-unfired reviews across the codebase with concrete file citations; (FR-005) audit currently-declared-and-fired reviews as the positive baseline (cycle-end distillation rule is the canonical instance); (FR-006) per-unfired-review trigger-class classification; (FR-007) recommended advisory sequencing of follow-up OPPs; (FR-008) cross-references back to PRD-0004 and the OPP-0032 promotion-gate observation; (FR-009) SUMMARY.md + docs/README.md index updates. Two should-have FRs: forward-pointer from cycle-end-distillation.md to session-shape.md (FR-010), one-line reference in harness-governance SKILL.md (FR-011). The Out of Scope table explicitly excludes: implementing any new companion rule (per-rule PRD passes); modifying any existing companion rule; auditing review *quality* (cargo-cult satisfier risk — separate follow-up); forcing a single canonical session shape on consumers (the doc is advisory, not prescriptive); renaming cycle-end-distillation.md; promoting candidate-stub-with-promotion-criterion to operating-principles § 9 in this PRD (bar is two observed firings — currently one). PRD takes positions on six open questions and includes a "v1 covers current state; v2 evolves with the catalog" framing for taxonomy maintenance. `candidates.md` OPP-0032 row bumped to `(exploring; PRD-0013 in flight, drafted same-day)`. SUMMARY.md gains PRD-0013 entry. `docs/README.md` PRD table extended; OPP-0032 status flipped to `exploring`. Implementation deferred to a follow-up PR. | Same-day OPP→exploring+PRD draft per the established cadence. The taxonomy is the *prerequisite* for the per-PRD passes coming for the OPP-0027..0031 cluster — drafting it first means those PRDs can cite the trigger-class vocabulary by name rather than each reinventing the framing. The workflow-doc-only scope-bias is load-bearing: the cost of bundling rule implementations into this PRD is conflating *design work* (deciding which review wants which trigger primitive) with *implementation work* (writing each rule's regex, satisfier set, humanReview text), which operating-principle § 7 (Align File Boundaries with Change-Class Boundaries) explicitly argues against. The session has now exercised the *deferred-implementations* discipline twice: PRD-0011 rejecting Option B (no synthetic trigger added to operating-principles); PRD-0013 deferring all per-rule machinery. Paired observation appended to `shared-observations.md`: *"Deferring rule implementations from the taxonomy PRD preserves the per-rule OPP→PRD discipline"* — names the two-instance pattern explicitly and proposes operating-principles § 9 promotion after a third instance. This row satisfies the kernel + opportunity-capture audit-trail floor + the cycle-end distillation rule's trigger on the OPP status flip and the PRD file creation. | @unclenate | PRD-0013, OPP-0032 |
| 2026-05-25 | Discovery | Filed [OPP-0032 — Session-Cycle Orchestration and Review-Trigger Taxonomy](../opportunities/OPP-0032-session-cycle-orchestration.md), promoted from the candidate-stub previously in `candidates.md`. Two concrete instances accumulated in one session and triggered the promotion gate the stub set: (1) the distilled-learnings dormancy resolved by [OPP-0026](../opportunities/OPP-0026-distilled-learnings-disposition.md) / [ADR-0014](../adr/ADR-0014-sunset-distilled-learnings.md) / [PRD-0011](../requirements/PRD-0011-distilled-learnings-disposition.md) — a declared "dedicated review session" that no automation fired in 40 days; (2) the Tula two-pass discovery captured in [OPP-0027..0031](../opportunities/OPP-0027-frontier-agent-posture.md) plus its paired observation *"Brownfield catalog gaps surface in layers"* — a declared "orthogonal-framing second pass for non-trivial consumers" that no automation scheduled, surfacing only when the maintainer re-read the Tula README. Both instances share the *same structural shape*: a declared review process with a clear forcing-mechanism need, but no automation actually firing it. The harness has positive-template evidence too — the cycle-end distillation rule (PRD-0004) fired correctly on PRs #60/#61/#62 and forced substantive distillations into being each time. OPP-0032's v1 scope-bias is **workflow-doc-only**: produce a taxonomy of session-boundary review checkpoints (session start, work, PR open, PR merge, post-merge, periodic-review, external-event-driven) naming which automation fires at each, audit currently-declared-but-unfired reviews across `cycle-end-distillation.md` / `harness-onboarding/SKILL.md` / `knowledge-capture/README.md` / `operating-principles.md`, and per-review decide v1-vs-v2 whether to propose machinery. Likely v1 output: a new peer workflow doc `platform/workflow/session-shape.md`. Per-rule PRD passes for any new companion rules the taxonomy recommends — preserving the OPP→PRD→implementation cadence per rule, avoiding "ship taxonomy and four rules in one PR." `candidates.md` paragraph rewritten from candidate-stub to filed-OPP row pointing at OPP-0032. `docs/README.md` OPP table extended; `SUMMARY.md` OPP list extended. Status: proposed; Confidence: medium-high. | This is the second instance of the **anchor-satellite filing pattern's promotion gate clearing in one session** — OPP-0007 in the earlier window (canonical-position, with five sibling observations becoming follow-up OPPs), and the candidate-stub-to-OPP promotion here. The discipline named in the OPP itself — *"declared reviews need forcing mechanisms or they silently die"* — is the same architectural class as the declaration-without-enforcement observations captured earlier in the session, but pointed at *review processes* rather than *artifacts*. The session-cycle taxonomy work also composes naturally with several active OPP-cluster items: it would inform Phase 2 of ADR-0013 (the vocabulary work), the per-OPP PRD passes for the OPP-0027..0031 cluster (which will exercise session-boundary review pressure heavily), and the `harness-onboarding` SKILL enhancement the Tula-two-pass observation already recommended. Implementation deferred to a future PRD pass. Paired observation appended to `shared-observations.md`: *"The candidate-stub-to-OPP promotion gate worked end-to-end on first firing"* — substantive distillation about *the promotion-gate that produced this OPP*, not cargo-cult padding (the connection between trigger work and distillation is direct: the OPP exists *because* the gate fired). This row satisfies the kernel + opportunity-capture audit-trail floor + the cycle-end distillation rule's trigger on the OPP file. The candidates.md edit is organizational per ADR-0012. | @unclenate | OPP-0032 |
| 2026-05-25 | Documentation | Closed audit finding **M-f** — `platform/templates/README.md`'s directory map was stale on the three templates added in PR #55 (v0.5.2). Added an **Eval strategy** row to the Testing table (`management/eval-gated-testing`) plus two new sections: **Skills** (`Skill authoring conventions` → `architectures/agent-skill-pack`, optional) and **Deployment** (`Self-hosting guide` → `delivery/self-hosted-oss`). Templates count unchanged (no new files) — index-completeness only. | The three templates shipped in PR #55 with `SUMMARY.md` entries, but their `templates/README.md` directory-map rows were missed — an instance of the M-f / M-j list-completeness drift class the 2026-05-25 audit named (a hand-maintained index falling behind the on-disk set). Closing it keeps the templates reference accurate ahead of the broader Phase 4 templates/module-README standardization. No companion rule requires this (templates/README.md is not a trigger path); logged for traceability of the audit finding. | @unclenate | — |
| 2026-05-25 | Discovery | **Tula second-pass cluster filing — five new OPPs (OPP-0027 anchor + OPP-0028..0031 satellites) + three augmentations.** Second-pass profiling of the Tula README on 2026-05-25 against a different framing question than the first pass ("what enterprise-AI-platform infrastructure does this commit to?" vs. the first pass's "what product shape and safety constraints apply?") surfaced a coherent cluster of catalog gaps the first pass missed entirely. Filed as an **anchor + four satellites** following the OPP-0007 precedent: **[OPP-0027 (anchor)](../opportunities/OPP-0027-frontier-agent-posture.md)** — `management/frontier-agent-posture` overlay declaring a project commits to enterprise-AI-platform standards from skill #1 (Microsoft Foundry agent patterns). **[OPP-0028](../opportunities/OPP-0028-ai-foundry-target.md)** — `architectures/ai-foundry-target` with enumerated `foundries:` field (Microsoft / Azure AI Foundry, NVIDIA AI Foundry, Palantir Foundry/AIP, AWS Bedrock Agents, Google Vertex Agents, IBM watsonx Orchestrate). **[OPP-0029](../opportunities/OPP-0029-agent-observability.md)** — `architectures/agent-observability` with required `docs/observability/trace-contract.md` following the Microsoft + Cisco Outshift multi-agent OpenTelemetry semantic conventions. **[OPP-0030](../opportunities/OPP-0030-intelligent-model-routing.md)** — `architectures/intelligent-model-routing` with required `docs/architecture/model-routing.md`; healthcare-specific models (MedGemma 4B/27B, MedASR, MedImageInsight, CXRReportGen) named in the suggested-providers list. **[OPP-0031](../opportunities/OPP-0031-agent-defense-in-depth.md)** — `architectures/agent-defense-in-depth` capturing Microsoft's four mutually-reinforcing patterns (agents-as-microservices, least-permissions, deterministic human-in-the-loop, agent-identity) with required `docs/security/agent-defense-in-depth.md` + `docs/security/append-only-action-log.md`. **Three substantive augmentations** to existing OPPs: [OPP-0015](../opportunities/OPP-0015-regulated-compliance-test-kits.md) gains *(B1)* BAA-tier LLM gateway governance and *(B2)* EU AI Act compliance test-kit integration (Microsoft Purview Compliance Manager, Credo AI, Saidot) as two named sub-shapes of the external-test-kit pattern. [OPP-0019](../opportunities/OPP-0019-eval-gated-testing-posture.md) gains the *three-stage eval lifecycle* dimension (dev → CI → production-traffic); v1's CI-stage coverage remains the floor, with production-traffic stage marked as a v2 OPP candidate. [OPP-0021](../opportunities/OPP-0021-delivery-self-hosted-oss.md) gains an optional `OPEN_CORE.md` template for self-hosted-OSS projects with a proprietary commercial extension (Tula/Aria as the reference instance). One paired process observation appended to `shared-observations.md`: *"Brownfield catalog gaps surface in layers — the first profile pass catches product-shape gaps; a second pass catches platform-layer gaps"* — generalizes the two-pass discipline as a process pattern; recommends `harness-onboarding` SKILL prompt for the framing question explicitly and budget two passes for non-trivial consumers. `candidates.md` gains a new sub-cluster *"From Tula second-pass — enterprise-AI-platform layering (2026-05-25)"* under the existing "Brownfield catalog coverage" cluster, paired with the existing first-pass "From Tula" sub-cluster. `docs/README.md` OPP table extended with OPP-0027..0031; `SUMMARY.md` OPP list extended. All five new OPPs at `proposed`; no PRD pass yet. | The first Tula pass (PR #53, 2026-05-24) caught the *product shape* and *healthcare-specific safety* dimensions — five OPPs (0018..0022) covering eval-gated skill-pack architecture, binary-eval testing posture, self-hosted-OSS delivery, evaluation tooling in toolchain, patient-facing health-agent safety. The second pass against the same project, after the catalog had digested the first-pass filings, caught the *enterprise-AI-platform layer* the first pass missed: foundry-targeting, observability, model routing, defense-in-depth. Both passes are correct; both are incomplete on their own. The discipline named in the paired observation generalizes: each brownfield onboarding pass should declare its framing question explicitly, and non-trivial consumers (especially agent-native ones) warrant at least one orthogonal-framing second pass. This is also a strong second instance for the *session-cycle orchestration candidate* in `candidates.md` — the missing review pattern named there is exactly "second-pass onboarding against a different framing question." The cluster's filing-as-anchor-plus-satellites is the second instance of the anchor-satellite pattern after OPP-0007; the pattern is now observed twice with concrete evidence. Implementation deferred to per-OPP PRD passes when each satellite's priority surfaces; the anchor OPP-0027 is the umbrella under which v1 PRDs would compose. This row satisfies the kernel + opportunity-capture + knowledge-capture audit-trail floor tripped by the five new OPP files + three OPP edits + shared-observations edit. | @unclenate | OPP-0027, OPP-0028, OPP-0029, OPP-0030, OPP-0031; aug OPP-0015, OPP-0019, OPP-0021 |
| 2026-05-25 | Governance | **PRD-0011 implementation shipped — `distilled-learnings.md` sunset, consolidated into `operating-principles.md`.** All 13 must-have FRs + 3 should-have FRs landed in one bundle. (FR-001..FR-004) `knowledge-capture/module.yaml` v1.1.0 → v1.2.0 — removed `docs/knowledge/distilled-learnings.md` from `requiredArtifacts`, removed it from rule #4's `requiredAny` satisfier list, removed rule #3 (the audit-trail rule that gated edits to the file), removed its `sensitivePaths` block. (FR-005) `docs/knowledge/distilled-learnings.md` rewritten as a one-paragraph dormancy pointer to `operating-principles.md`. (FR-006) `cycle-end-distillation.md` decision tree updated from three destinations to two. (FR-007) `knowledge-capture` README rewritten: two files in docs/knowledge/; agent-behavior step 4 updated to "promote patterns to operating-principles when they crystallize"; Companion Rules section updated (rule #3 dropped). (FR-008) `docs/knowledge/README.md` rewritten with new evidence-driven promotion section replacing the old Distillation Cadence. (FR-009) `docs/operating-principles.md` gained an explicit header note claiming the curated longitudinal destination role — **no synthetic trigger added**, per the discipline named in the prior sunset-PRD observation. (FR-010) [ADR-0014 — Sunset `distilled-learnings.md`](../adr/ADR-0014-sunset-distilled-learnings.md) accepted: records the decision, alternatives weighed (B revive, C clarify), rejection rationale, explicit revisit triggers. (FR-011) All 8 validators green; markdownlint clean on 311 files. (FR-012) Diagram 5 SATISFIERS subgraph reduced from 3 nodes to 2; historical note added. (FR-013) `HARNESS.md` knowledge-surfaces row updated to point at `operating-principles.md` with a historical pointer to the dormant file. (FR-014) Template-side dormancy stub mirror at `platform/templates/knowledge/distilled-learnings.md` with three migration options for existing consumers. (FR-015) `platform/templates/README.md` directory-map row marked dormant. **Bonus reconciliations** in the same bundle: `.markdownlint-cli2.jsonc` exclusions for both distilled-learnings.md files lifted (the audit-trail rule motivating those exclusions was just removed; dormancy stubs are ordinary markdown now). `SUMMARY.md` ADR list reconciled — added ADR-0013 (missed in the Phase 1 ship) and ADR-0014. `docs/README.md` ADR table + PRD table extended; OPP-0026 flipped exploring → accepted; PRD-0011 flipped Proposed → Accepted. Sample-project Stop-event hook (`platform/examples/sample-projects/node-web-saas-postgres/.claude/hooks/distillation-prompt.sh`) updated: regex now matches two destinations; prompt destination list updated. Paired observation appended to `shared-observations.md`: *"The distilled-learnings sunset shipped — the sunset-PRD discipline carried through to implementation"* — satisfies the PRD-0011 acceptance criterion AND the cycle-end-distillation rule fired by this PR's ADR + module.yaml + OPP edits. | **Bundling rationale:** the 16 FRs (13 must-have + 3 should-have) compose into a single well-scoped PR exactly as PRD-0011 anticipated. The discipline named in the prior session's sunset-PRD observation was **exercised concretely**: operating-principles.md gained the explicit curated-destination claim but no synthetic trigger was added — the failure mode that killed distilled-learnings was structurally ruled out at the surviving destination. The cycle-end-distillation rule's behavior is unchanged; only its satisfier surface area shrank. The markdownlint-exclusion and SUMMARY.md ADR-list reconciliations are direct instances of the M-j list-completeness drift class — fixed in-bundle for the same reason the Phase 0.5 bundle fixed SUMMARY.md's OPP-list drift. Same-day OPP→exploring+PRD→accepted+ADR→implementation cadence — fifth instance this session (after OPP-0004/0005/0006/0007). This row satisfies the kernel + opportunity-capture + knowledge-capture + ADR audit-trail floors tripped by the bundle's module.yaml + OPP + ADR + PRD + workflow edits. | @unclenate | PRD-0011, ADR-0014, OPP-0026 |
| 2026-05-25 | Scope | Promoted [OPP-0026](../opportunities/OPP-0026-distilled-learnings-disposition.md) `proposed` → `exploring` (same-day per the established maintainer-priority cadence — the OPP-0004/0005/0006/0007 pattern). One paired observation appended to `shared-observations.md` — *"Sunsetting a declared-but-unused mechanism must rule out replicating the failure mode at the surviving destination"* — capturing the structural insight behind PRD-0011's rejection of Option B (revive with forcing trigger); satisfies the cycle-end distillation rule that fires on OPP and PRD edits, which CI caught on the first push because the initial PR claim of "rule not triggered" was wrong. Disposition committed: **Option A — Sunset.** Drafted [PRD-0011 — Sunset `distilled-learnings.md`](../requirements/PRD-0011-distilled-learnings-disposition.md) as the paired design, covering 13 must-have FRs: (FR-001) remove the file from `requiredArtifacts` in `knowledge-capture/module.yaml`; (FR-002) remove it from the cycle-end-distillation rule's `requiredAny` satisfier list; (FR-003) remove the audit-trail rule that gates its edits; (FR-004) remove its `sensitivePaths` entry; (FR-005) rewrite the file itself as a one-paragraph dormancy pointer to `operating-principles.md`; (FR-006) update `cycle-end-distillation.md` decision tree to two destinations; (FR-007) update the `knowledge-capture` README; (FR-008) update `docs/knowledge/README.md`; (FR-009) conditional one-paragraph note in `operating-principles.md` claiming the curated-knowledge role; (FR-010) file **ADR-0014 — Sunset `distilled-learnings.md`** as the decision record; (FR-011) verify the harness's own self-dogfood validators stay green; (FR-012) update Diagram 5 (Distillation Trigger Composition); (FR-013) update HARNESS.md knowledge-surfaces row. Three should-have FRs cover the template-side mirror (FR-014), templates README update (FR-015), and an optional review-log entry (FR-016). PRD takes positions on six open questions and explicitly rejects Option B (revive with forcing trigger) and Option C (clarify dormant) for v1, with documented triggers for revisiting either if evidence emerges. `candidates.md` OPP-0026 row bumped to `(exploring; PRD-0011 in flight, drafted 2026-05-25)`; SUMMARY.md gains PRD-0011 entry; `docs/README.md` PRD table extended; OPP-0026 status flipped to `exploring`; Disposition populated. Implementation deferred to a follow-up PR (likely the v0.5.x patch-release window or v0.6.0 alongside PRD-0007). | Same-day OPP→exploring+PRD draft preserves maintainer-priority signal (per the OPP-0004 cadence pattern). Disposition direction was already committed in OPP-0026's Thesis with high confidence; the PRD operationalizes that commitment into a concrete 13-FR delivery plan and records the alternatives' rejection rationale durably. The rejection of Option B is load-bearing: it explicitly prevents the failure mode this PRD removes from `distilled-learnings.md` (a declared destination with a synthetic trigger no one wants) from being recreated against `operating-principles.md`. ADR-0014 is the canonical decision record; PRD-0011 specifies *what* changes and *why*; the implementing PR ships the *how*. Implementation deferred to keep this filing focused on the design commitment — the v1 deliverable is small (~13 FRs across ~8 files) and well-scoped for a single follow-up PR. This row satisfies the kernel + opportunity-capture + knowledge-capture audit-trail floor tripped by the OPP edit + PRD file + cycle-end-distillation rule. | @unclenate | PRD-0011, OPP-0026 |
| 2026-05-25 | Governance | **Phase 0.5 hotfix bundle from the 2026-05-25 documentation-audit refresh.** Combined mechanical fixes + two new OPPs + a session-cycle candidate stub + index reconciliation across SUMMARY.md. **(1) H-d resolved — 8 stale "7 validators" / "seven validators" references corrected** at `platform/validators/README.md:158` (also bumped the integration-test enumeration from 21 to 24 by adding `validate-catalog-counts.sh` to `VALIDATOR_SCRIPTS` in `test_validators_integration.rb`, closing a silently-missed `--help` test coverage gap from v0.5.0), `platform/examples/sample-projects/{submodule-consumer,node-web-saas-postgres}/docs/{project/milestones.md:27, ops/release-checklist.md:31}`, `docs/_assets/README.md:71` (also bumped `35 modules` → `43`, `56 templates` → `59`), `docs/project/dependency-log.md:17`, and `docs/architecture/diagrams.md:353` (Mermaid label). The CHANGELOG.md and audit-doc references are historical narrative and stay as-is. **(2) H-b resolved — `$PLATFORM` → `$PLATFORM_ROOT` standardized** across `platform/workflow/bootstrap-quickstart.md` (Step 2 now opens with an explicit definition block covering submodule-mode, in-tree-mode, and self-checkout-mode `$PLATFORM_ROOT` values, mirroring `ci-integration.md`'s convention) and `platform/workflow/troubleshooting.md` ("Running all validators at once" recipe rewritten with `$PLATFORM_ROOT` plus a forward-pointer to bootstrap-quickstart.md Step 2; recipe extended to cover all 8 validators including doc-references and catalog-counts that the audit's M-i flagged as missing from the chain). **(3) `submodule-integration.md` gained Step 6 — Verify fresh-clone integration** — a new section in the Quick start flow naming the two silent-failure modes (`.harness/` empty after clone-without-recurse-submodules, and SHA-unreachability post-force-push/branch-deletion), with a smoke-test recipe consumers run from a clean directory. Acknowledges PR #58's `-b main` improvement as a complementary intent-clarifier that does not provide a mechanical check. **(4) Two paired observations appended to `shared-observations.md`:** (a) *"Cross-repo declarations have the same silent-drift failure mode as intra-repo doctrine-without-enforcement, and the harness's own CI cannot see them"* — frames the smoke-test gap as the cross-repo instance of the project's already-named "doctrine in prose without enforcement in code" pattern; (b) *"Declared knowledge surfaces without an inbound-flow trigger silently die; operating-principles ate distilled-learnings' lunch"* — the intra-repo sibling, triggered by the maintainer's mid-bundle question about distilled-learnings.md (40 days of zero content despite being a declared `requiredArtifact`). **(5) Two new OPPs filed:** [OPP-0025](../opportunities/OPP-0025-consumer-integration-smoke-test.md) (consumer-side integration smoke test as a first-class harness primitive — CI template + workflow recipe) and [OPP-0026](../opportunities/OPP-0026-distilled-learnings-disposition.md) (sunset / revive / clarify the dormant `distilled-learnings.md`). **Both filed originally as OPP-0023 / OPP-0024 in working tree, renumbered to 0025 / 0026 after PR #59 took the OPP-0023 slot mid-bundle** for the separate Tula-surfaced `validate-doc-references` consumer-aware finding (same renumbering pattern as the OPP-0006 → OPP-0007 collision earlier this session; documented in the renumbered OPPs' Last Updated notes). **(6) Session-cycle orchestration candidate stub** added to `candidates.md` capturing the maintainer's framing: *"there's an as yet undefined optimal set of process steps for a session, and we are missing reviews that could be triggering these powerful automations we've designed."* Frame the ideal session shape end-to-end (start → work → close), produce a taxonomy of review triggers (PR-boundary, session-boundary, time-boundary, count-boundary, audit-boundary), and identify which automations are declared-but-unfired. Held as candidate-stub pending a second concrete instance. **(7) SUMMARY.md index reconciled** — added OPP-0018, 0019, 0020, 0021, 0022 (all previously missing from the OPP list since the 2026-05-24 Tula filing batch), plus the three new OPPs (0023, 0025, 0026). This is itself a direct instance of the M-j list-completeness drift the audit flagged: an index file that hand-lists entries had silently fallen behind the on-disk OPP file set. `docs/README.md` OPP table also extended with 0023 / 0025 / 0026. All validators green after the bundle: catalog-counts (23/23 assertions); placeholders; doc-references. | **Bundling rationale:** all six pieces compose. H-d + H-b are the audit's remaining live mechanical findings on the "do only five things" list (U1 and H-c were already closed by Phase 1). The smoke-test section + OPP-0025 + observation A close the cross-repo silent-failure gap surfaced by the maintainer's fresh-clone-initialization insight earlier in the bundle. The distilled-learnings investigation + OPP-0026 + observation B + session-cycle stub close the intra-repo sibling gap surfaced by the maintainer's mid-bundle question. The SUMMARY.md reconciliation is a direct instance of the M-j drift class — fixing it in this same bundle is the "bundling truth-fixes with catalog growth" pattern from PR #56 applied to a hotfix context. The OPP renumbering (0023→0025, 0024→0026) is the second occurrence of the maintainer-parallel-PR-number-collision pattern this session and reinforces the durable feedback memory that says: fetch open PRs before substantive filings; defer to maintainer's framing when there's a slot conflict; renumber and note the collision in the OPP's metadata. Phase 0.5 closes the "audit refresh said five urgent things; current state and Phase 1 collapsed it to three; this bundle ships those three plus the bonus structural-gap captures the investigation surfaced." Phases 2-4 of ADR-0013 still queued; PRD-0007 implementation still queued (v0.6.0); PRD-0006 still queued (v0.7.0). | @unclenate | ADR-0013, OPP-0025, OPP-0026 |
| 2026-05-25 | Documentation | **Ingested documentation-audit Refresh #1** as `docs/QUALITY-AUDIT-2026-05-25-documentation-refresh.md` (paralleling `QUALITY-AUDIT-2026-05-18.md` and `QUALITY-AUDIT-2026-05-24-documentation.md`). Source: `documentation-audit-2026-05-24/auto-harness-documentation-audit-refresh-2026-05-25.md` (untracked, authored by the maintainer against `bf7265a` / v0.5.2 before PR #57 merged). Audit relocated, source directory removed, `docs/README.md` Security & quality section extended with the new entry. **Audit findings vs. current state (verified at ingest):** U1 (markdownlint `<strong>` blocker) — already closed; the offending `<strong>` tag was removed during Phase 1 implementation before PR #57 merged. H-c (PRD-0008/9/10 missing from `docs/README.md`) — already closed; Phase 1 added all three rows. H-d (8 unwatched "7 validators" sites) — confirmed still live: `platform/validators/README.md:158`, `platform/examples/sample-projects/{submodule-consumer,node-web-saas-postgres}/docs/{project/milestones.md:27, ops/release-checklist.md:31}`, `docs/_assets/README.md:71`, `docs/project/dependency-log.md:17` (the CHANGELOG.md and audit-doc references are historical and stay as-is). M-j (list-completeness drift as an unguarded validation class) — confirmed structural; H-c and M-f are symptoms of the same gap. H-b (`$PLATFORM` vs `$PLATFORM_ROOT`), H-e (examples-README documents 1 of 5 samples), H-f (diagrams #4..#11 unembedded), M-a..M-i — all confirmed open, slated for Phases 2-4 of ADR-0013. **Phase 0.5 hotfix opportunity:** the audit's §3 / §10 "do only five things" list collapsed in scope after ingest verification: U1 + H-c are already closed; H-d remains as a clean ~10-minute find-and-replace pass; H-b remains as the last Phase 0 item. **Surfaced as candidate (not yet filed; the next free OPP slot after the v0.5.x renumbering):** *Validator extension for list-completeness assertions* — generalizes `validate-catalog-counts.sh` from integer-watching to also assert that named indexes (PRD table in `docs/README.md`, template directory map in `templates/README.md`, sample-project list in `examples/README.md`) have an entry per file-on-disk. This is the M-j structural fix and the audit's single highest-leverage recommendation. No artifact changes beyond the relocation, index update, and this entry. | The audit was authored before PR #57 merged, so its "urgent" U1 and "still-stale" H-c items were rendered moot by the time of ingest; capturing this discrepancy in the change-log preserves the audit-vs-current-state diff so the document itself can be read as-of-its-write-time without misleading future readers. Relocating into `docs/` follows the established pattern (paralleling the 2026-05-18 and 2026-05-24 audits already under governance); kept the audit content unmodified (audits are author-against-state artifacts; current-state context belongs in the ingest entry, not in the audit prose). The structural finding M-j — list-completeness as a distinct validation class — is exactly the kind of OPP candidate the opportunity-capture module exists to surface; filing it as a real OPP is a clean next step after the Phase 0.5 hotfix lands. | @unclenate | ADR-0013 |
| 2026-05-25 | Discovery | Filed OPP-0023 — `validate-doc-references.sh` hard-fails (exit 2) for submodule consumers because it requires a `<root>/platform/` tree and never reaches its general markdown-link-resolution pass on consumer docs; and the consumer CI template (`platform/templates/ci/github-actions.yml`) includes a doc-references step that `platform/workflow/ci-integration.md`'s minimal workflow omits — so following the template reds a consumer's CI. Surfaced by the Tula brownfield onboarding when wiring the validator chain into CI (recorded consumer-side in tula `docs/adr/ADR-0002-ci-validator-gate.md`, which excludes the step as a workaround). Initial bias: make the validator scan consumer `*.md` when no `platform/` exists (skip the platform-specific Pass 1), keep the dogfood self-run unchanged (auto-harness has `platform/`), align the template + guide, and treat "nothing to scan" as exit 0 (reserve exit 2 for genuine misuse). `candidates.md` adds OPP-0023 under "Consumer onboarding & project hygiene"; one paired observation appended to `shared-observations.md`. Status `proposed`; Confidence high. | The first submodule consumer to wire the harness validator chain into CI (Tula) hit this on the first run — a validator that hard-requires the harness's own repo layout is a concrete onboarding stumbling block, and the v0.5.2 agent-native batch just made the harness more attractive to exactly this consumer class. Filed as an OPP (not a direct fix) because the fix shape — make-validator-consumer-aware vs. remove-from-template — is a small design choice the maintainer should ratify, and a validator behavior change warrants the OPP→PRD discipline plus a no-`platform/` self-test fixture. This row is the audit-trail floor for the OPP edit; the paired observation satisfies the distillation rule. | @unclenate | OPP-0023 |
| 2026-05-25 | Documentation | Phase 1 of ADR-0013's documentation IA work — README front-door rebuild. (1) H1 wordmark fixed: `Development Harness` → `auto-harness` (closes audit finding M2 — name dissonance between repo, badges, and H1). (2) Hero SVG embedded at the top of README.md (referencing `docs/_assets/proposed-visuals/hero-before-after.svg`, the proposed mockup that shipped with the audit and was relocated to its current path in PR #56). (3) Value section restructured: one-line bolded value claim + concrete `harness.manifest.yaml` example *above* the TOC; the abstract opening blurb replaced with a "without it / with it" framing that names the failure mode the harness prevents. (4) "What It Does" and "Who This Is For" promoted above the TOC, per the audit's Phase 1 recommendation; the 17-row flat TOC collapsed into a `<details>` block titled "Full table of contents — 17 sections; expand for navigation" (closes audit findings C1 — "buried value" — and M1 — "README is ~570 lines doing four jobs"). (5) The 5-way adoption-path branch moved from *above* the value (where it forced a fork before the reader had context) to *below* the value section as "Adoption paths" — context-before-fork. The submodule-integration path added as the recommended consumption pattern (was missing from the original 5-way list; closes audit C4). (6) Mermaid Diagram 1 (Component Composition) embedded directly in the "How It Works" section, sourced from `docs/architecture/diagrams.md` § 1 (closes audit H1 — "visuals exist but aren't wired into concept docs"). (7) "New here? Start with the README" banners added to `HARNESS.md` and `SUMMARY.md` — signposts at non-README entrypoints route newcomers to the right starting page (per audit Phase 1 recommendation). `SUMMARY.md` H1 also flipped from `Development Harness` to `auto-harness` for wordmark consistency. PR cites ADR-0013 in its commit message and PR body; the kernel/base governance-entrypoint companion rule is pre-empted by ADR-0013 (no per-commit ADR or change-log stub needed beyond this entry). Validators green; markdownlint clean (1 inline-HTML finding fixed by removing a stray `<strong>` tag inside the `<details>` summary, which isn't in the allowed-elements list). One paired process observation captured: *"Documentation reorder costs almost nothing and unblocks the audience the docs were already serving"* — generalizes the "reorder, not rewrite" pattern that Phase 1 exercised. Net new prose: ~2 sentences. Net structural change: substantial. | The audit's headline finding ("The hook exists. It is just buried under the navigation.") was the diagnosis Phase 1 specifically exists to fix. This PR executes that fix exactly — wordmark + hero + value + concrete example + promoted substance + collapsed TOC + demoted fork + embedded diagram + signposts. Each piece is small; the cumulative effect is the 60-second test no longer fails. Phase 2 (mental model click — Core Concepts block, vocabulary, expanded trust-model.md, doctrine rationale) follows in a subsequent PR under the same ADR-0013 umbrella. | @unclenate | ADR-0013 |
| 2026-05-25 | Scope | Promoted OPP-0018 / OPP-0019 / OPP-0021 `proposed` → `accepted` and shipped them as v1 catalog modules under the **v0.5.2 patch release-marker** (Tula agent-native delivery; pure catalog growth, sibling to the v0.5.1 YouBase patch), with paired PRD-0008 / PRD-0009 / PRD-0010. New modules: `architectures/agent-skill-pack` (authored eval-gated skill pack as a delivery topology; requires `docs/architecture/overview.md`; companion rule binds skill-source changes to a matching eval); `management/eval-gated-testing` (sibling to `testing-standard`; binary-graded eval gate; requires `docs/testing/eval-strategy.md`); `delivery/self-hosted-oss` (posture between prototype and production-saas; requires `docs/deployment/self-hosting-guide.md`; declares no conflicts per the `internal-platform` precedent). Three new templates (`skills/authoring-conventions`, `testing/eval-strategy`, `deployment/self-hosting-guide`). Catalog counts: modules_profiles 26→29, modules_all 35→38, templates 56→59 (every assertion site bumped). Propagated to SUMMARY Module Library + PRD list + template sections; `harness-onboarding` SKILL catalog (architectures / delivery / management); `discovery-to-composition` Step 6 rubric; `roadmap.md` (new v0.5.2 section after v0.5.1). Evidence: tula gap analysis §TG1 / §TG2 / §TG3. One distillation observation in `shared-observations.md`. | Maintainer chose to flesh out the three **unblocked** agent-native OPPs from the Tula first pass into modules so the second-pass tula intake profiles against a real catalog rather than one bounded by "no module fits". Marker is **v0.5.2, not v0.8.0**: the 2026-05-24 prioritization examination (#54) anchored v0.8.0 at OpenEMR Phase 1, and this batch is the same pure-catalog-growth / zero-machinery class as the v0.5.1 YouBase patch, so it ships in the patch lane rather than competing with v0.6/v0.7. The other three Tula OPPs stay deferred behind OPP-0013/0016/0022's **US-healthcare-bias guardrail** (international second-evidence required before freezing healthcare artifacts): OPP-0022, OPP-0020, and the OPP-0013 healthcare-fhir/smart sub-modules. Sibling module (not a `testing-standard` mode-field) and no-conflict delivery posture (not symmetric edits to prototype/production-saas) chosen to keep the batch zero-validator-risk; both divergences from the OPP biases are recorded with rationale in the PRDs. This row satisfies the kernel (module.yaml → change-log), opportunity-capture (OPP edits), and knowledge-capture (shared-observations) audit-trail companion rules the batch trips. | @unclenate | PRD-0008, PRD-0009, PRD-0010, OPP-0018, OPP-0019, OPP-0021 |
| 2026-05-25 | Governance | **v0.5.1 "this-week bundle" shipped.** Outcome of the 2026-05-24 prioritization examination (3 parallel agents) that ingested the Tula brownfield batch (OPP-0018..0022) and the standalone documentation audit (`docs/QUALITY-AUDIT-2026-05-24-documentation.md`). Four coordinated pieces in one PR per the agents' "ship-this-week" recommendation: (1) **YouBase catalog growth** — 5 new modules under `platform/profiles/` accepted from OPP-0008/0009/0010: `stacks/node-javascript`, `stacks/coffeescript`, `data/embedded-key-value`, `data/browser-storage`, `domains/cryptographic-identity`. All zero required artifacts. OPP statuses flipped `proposed → accepted` with `Last Updated` notes. Modules added to `SUMMARY.md` Module Library + `candidates.md` status notes + `docs/README.md` OPP/PRD index. (2) **Phase 0 factual fixes from the documentation audit** — `platform/reference/glossary.md` line 66 corrected from "Six validators" to "Eight validators" with full enumeration; `HARNESS.md` line 14-16 corrected from "six Mermaid diagrams" to "eleven"; `platform/workflow/submodule-integration.md` line 31 "seven harness validators" → "eight"; `ADR-0004` status flipped `Proposed → Accepted` with a dated note; new stub `docs/architecture/overview.md` resolves the 15+ dangling references across `SUMMARY.md`, multiple ADRs/PRDs/OPPs, workflow guides, profile READMEs, skills, and templates; `submodule-integration.md` added as the *recommended* adoption path in `how-to-read.md` and `index.md`. (3) **ADR-0013 — Documentation Information Architecture** drafted as the governance umbrella for the 5-phase documentation improvement plan; pre-empts the kernel/base governance-entrypoint companion rule for all subsequent README/HARNESS/AGENTS edits across Phases 1-4 (per the audit § 10 recommendation to author one ADR rather than scatter change-log stubs). (4) **`docs/README.md`** new file — ADR/PRD/OPP index covering all 13 ADRs + 7 PRDs + 22 OPPs with status and cross-references. Auxiliary work bundled in: documentation audit relocated from `documentation-audit-2026-05-24/` (untracked working-tree location) into `docs/QUALITY-AUDIT-2026-05-24-documentation.md` (paralleling the existing `QUALITY-AUDIT-2026-05-18.md`); proposed SVG mockups relocated to `docs/_assets/proposed-visuals/`; SVG references in the audit doc rewritten to match new paths. Catalog counts bumped to reflect 5 new modules: `modules_profiles 26→31`, `modules_all 35→40` — caught by `validate-catalog-counts.sh` at four assertion sites (`how-to-read.md` prose + ASCII art, `diagrams.md` diagram-1 label, `cover-back.svg`), fixed in same PR. One paired process observation captured: *"Bundling truth-fixes with catalog growth produces a higher-confidence shipping unit"* — generalizes the bundling pattern (truth-repair + content addition under one governance umbrella) for future multi-piece prioritization decisions. | Outcome of the 2026-05-25 prioritization examination committing to Option γ (parallel with brownfield-first bias) with the this-week bundle as the immediate move. Per the agents' analysis, this bundle is unambiguously the right immediate step regardless of longer-term sequencing — zero risk, all upside, ships cheap. The bundling rationale is structural (not cargo-cult): Phase 0's count fixes touch the documentation the new modules cross-reference, and ADR-0013 is the governance umbrella under which both halves sit. Sets v0.5.1 as the release target once this PR merges. Wave 2-4 of the documentation audit (Phase 1 README rebuild, Phase 2 vocabulary, Phase 3 visual program, Phase 4 navigation hygiene) will land as subsequent PRs interleaved with PRD-0007 (v0.6.0) and PRD-0006 (v0.7.0) implementation per Option γ. The OPP-0018..0022 Tula cluster slots into v0.6/0.7 alongside OpenEMR Phase 1 per the agent C analysis (minimum-viable subset OPP-0018+0019+0021 = ~2-3 weeks). | @unclenate | ADR-0013, OPP-0008, OPP-0009, OPP-0010 |
| 2026-05-24 | Scope | **Prioritization examination committed; v0.5.1 patch added; v0.8.0 anchored at OpenEMR Phase 1.** Examination conducted via three parallel sub-agents analyzing the YouBase OPP cluster (OPP-0008/0009/0010), the OpenEMR OPP cluster (OPP-0011..0017), and cross-cutting sequencing against PRD-0006/PRD-0007. Key findings synthesized: (1) **YouBase cluster is ~3 hours of engineering, pure catalog growth, zero machinery.** All three OPPs ship zero required artifacts; the cluster slots in as a v0.5.1 patch without disrupting the v0.6/v0.7 sequence. (2) **OpenEMR cluster is structurally larger** (7 OPPs, 16-24 weeks for full cluster) with an internal dependency tree where OPP-0013 (healthcare family) is the anchor and OPP-0014/0015/0016/0017 are satellites — a textbook second instance of the anchor-satellite pattern from OPP-0007. Phase 1 minimum-viable = OPP-0011 (PHP) + OPP-0012 (SQL generalization) + OPP-0013 core (5 of 12 sub-modules); ~4-6 weeks. (3) **PRD-0006 ↔ PRD-0007 swap is technically reversible but operationally not** — the cross-cutting analysis identified that Observation A (which connects trust-tier to canonical-position) is a *follow-up* OPP, not part of v1 of either PRD; at v1 the two are independent. Re-swapping a day-old decision is decision-thrash. Sequence committed: **v0.5.1 YouBase patch → v0.6.0 PRD-0007 → v0.7.0 PRD-0006 → v0.8.0 OpenEMR Phase 1 → v0.9+ OpenEMR Phase 2/3 + sibling-observation follow-ups (A, B, D)**. Roadmap.md updated to reflect the full sequence with effort estimates per phase. v0.5.1 added as a new section ahead of v0.6.0; v0.8.0 expanded with OpenEMR Phase 1 detail; v0.8+ section retained for the sibling-observation follow-ups (renamed for clarity). | The maintainer requested an examination after the brownfield OPP filings (PRs #49, #50) landed during the in-flight priority-swap PR (#51), since those 10 new OPPs hadn't been weighed in the earlier examination. Spinning up three parallel agents (rather than serial analysis) gave: (a) faster turnaround, (b) genuinely independent analysis per cluster, (c) the cross-cutting agent forced the dependency tree explicit. The single-best-pick recommendation (Option A — YouBase patch + retain swap + add OpenEMR Phase 1 at v0.8) wins on three axes: cheap quick win (YouBase), respects the recent decision (no thrash), and treats OpenEMR with the dedicated PRD-pass discipline its scope warrants. The decision is captured in the roadmap + this change-log entry as a single epistemic act rather than spread across multiple smaller PRs — the analysis happened in one examination and lands in one record. | @unclenate | — |
| 2026-05-24 | Scope | **Swapped implementation order: PRD-0007 (canonical-position) ahead of PRD-0006 (trust-tier-enforcement).** v0.6.0 release-marker is now canonical-position; v0.7.0 becomes trust-tier-enforcement. Original plan was the reverse. Rationale (per the audit-trajectory examination 2026-05-24): (1) **Field evidence beats audit evidence** — OPP-0007 came from a real consumer (`bdits/municipal-brain`) hitting the canonical-position gap in production this week; PRD-0006 came from my audit, which is good signal but lower than active consumer use. (2) **Smaller initial scope** — PRD-0007 is a new opt-in module + two templates + two companion rules; PRD-0006 is schema migration + new validator + dogfood pass across 9 existing modules + multi-doc updates. (3) **Better downstream unblocking** — three follow-up OPPs (Observations A, B, D) depend on canonical-position as prerequisite; trust-tier v1 doesn't unblock anything similar. (4) **Trust-tier v2 (session-level enforcement) actually wants canonical-position citations** as the rationale surface for validator opt-outs per Observation A; doing trust-tier first means redoing the override-rationale design later when canonical-position lands. Version-markers updated in both PRDs to reflect the new order; PRD-0007 now targets v0.6.0; PRD-0006 targets v0.7.0; both PRDs gained explicit "swapped 2026-05-24" notes pointing here. Documentation companion updates landed in the same PR: (a) two new diagrams in `docs/architecture/diagrams.md` — Diagram 10 (Canonical-Position Artifact Flow: visualizes citation rule + ratification rule + review-artifact composition) and Diagram 11 (Anchor-Satellite Filing Pattern: visualizes the OPP-as-anchor + satellite-observations + bundled-vs-deferred decision tree); diagram count 9→11. (b) Back cover SVG diagrams count bumped 9→11. (c) New `docs/roadmap.md` surface — released (v0.5.0) / planned (v0.6.0 canonical-position, v0.7.0 trust-tier, v0.8+ follow-ups) / toward-v1.0; cross-linked from SUMMARY.md Start Here. (d) Cross-link added from `platform/workflow/extending-the-harness.md` to the anchor-satellite pattern. (e) PRD-0007 gains Cross-references entry pointing at Diagram 10. **Caveat:** this swap analysis pre-dated PRs #49 (OPP-0008..0010 YouBase brownfield) and #50 (OPP-0011..0017 OpenEMR brownfield) which merged while this work was in-flight on a separate branch. Those 10 new OPPs represent additional priority candidates that the next examination should ingest — particularly the brownfield catalog-coverage cluster which may unblock more consumer adoption than either PRD-0006 or PRD-0007 individually. Roadmap surface to be updated in a follow-up to reflect the brownfield-OPP backlog explicitly. | The audit-trajectory examination requested by the maintainer pre-empted the next implementation push (originally going to be PRD-0006). Re-prioritization decisions are themselves part of the audit-driven cadence — capturing the swap with explicit rationale lets future readers (or future-me) reconstruct why the order changed. The documentation companion updates (diagrams, roadmap, cross-links) were bundled in the same PR because they reflect the same epistemic shift. The brownfield-OPP caveat is honest about the analysis's incomplete information — the next prioritization examination should explicitly weigh the brownfield work against PRD-0006/PRD-0007. | @unclenate | PRD-0006, PRD-0007, OPP-0006, OPP-0007 |
| 2026-05-24 | Governance | Added a Markdown-table authoring caveat to `docs/operating-principles.md` § 8 (Prefer Text Representations): never put a raw pipe inside an inline code span inside a Markdown table cell — the renderer parses it as a column delimiter, silently breaking the table (markdownlint MD056) and cascading into spurious MD038 code-span warnings. Documents the three safe alternatives (slashes, prose, escaped pipes) and cites the concrete incident that motivated it: the OPP-0011..0017 batch's change-log row carried a four-engine enum code span whose pipes reported nine columns where six were expected, fixed twice during PR #50. Framed as a corollary of § 8 (Markdown stays the right choice; the table sub-syntax has a trap worth naming), not an exception to it. | Cheap-satisfier discipline (ADR-0010): this is synthesis of a confirmed, twice-observed authoring failure into a durable citable rule, not a new design decision — so no ADR/PRD is spawned and a change-log entry is the appropriate weight, exactly as the § 8 addition itself (2026-05-23) was handled. Canonizing it in operating-principles (rather than siloing it in an agent's private memory) keeps the learning in the git-tracked, shareable store per the harness's own "private memory is a cache, not the canonical store" doctrine. | @unclenate | — |
| 2026-05-24 | Discovery | Filed five new candidate OPPs (OPP-0018..0022) and augmented two existing healthcare OPPs (OPP-0013, OPP-0016) from the **Tula** brownfield onboarding — the third external first-pass of 2026-05-24 (OpenClaw personal-health-agent skill pack, fork at `unclenate/tula`). Originating evidence: consumer gap analysis at tula `docs/knowledge/harness-coverage-gap-analysis.md` (§TG1..TG6). Distinct gap class from the YouBase/OpenEMR stack-breadth batches — **delivery-topology breadth for agent-native products**. OPP-0018 (authored eval-gated agent **skill-pack** as a delivery topology; bias `architectures/agent-skill-pack` + thin `domains/openclaw`). OPP-0019 (binary-eval quality gate as a consumer **testing posture**; bias `testing-standard` `mode: eval-gate`). OPP-0020 (**evaluation & safety tooling** — Waza / GAIA / UK-AISI Inspect — as auto-harness *toolchain components*, not mere awareness; inbound complement to OPP-0001; explicit maintainer signal). OPP-0021 (`delivery/self-hosted-oss` posture, between `prototype` and `production-saas`). OPP-0022 (**patient-facing health-agent safety** — triage gating, draft-never-send, non-diagnostic stance, PHI workspace boundary, indirect-injection-via-ingestion — patient-side counterpart to OPP-0013's operator-side family). Augmentation: OPP-0013 + OPP-0016 gain Tula as the role-distinct **second healthcare consumer** (patient-authorized SMART client vs OpenEMR's server/provider-launch) plus a **US-healthcare-bias guardrail**. `candidates.md` gains a "From Tula" sub-cluster and is reframed as a 3rd brownfield batch. Two architectural observations appended to `shared-observations.md`: (a) agent-native delivery as a second catalog-gap class distinct from stack/data breadth; (b) US-healthcare bias — both healthcare evidence points are American (OpenEMR ONC/HIPAA; Tula Epic-MyChart/HIPAA §164.526/US-Core), international second-evidence (EHDS, Near/Far East; OpenMRS) required before freezing any healthcare artifact. All five OPPs `proposed`; no PRD pass. | Third external brownfield onboarding closes the discovery loop on a consumer kind the prior two did not exercise: an agent-native product whose unit of delivery is an eval-gated skill pack on a runtime, not an app or a service. The catalog's conventional layers described Tula's surface cleanly; the miss concentrated entirely in delivery topology — evidence that the next catalog-breadth investment is agent-native delivery, not more language stacks. The Waza-as-component direction (OPP-0020) was an explicit maintainer signal during the pass and is the inbound complement to OPP-0001's outbound governance export. Healthcare was deliberately NOT re-filed from scratch — OpenEMR already covers it operator-side; Tula augments OPP-0013/0016 as the patient-side second consumer OPP-0013's own Risks section asked for. The US-bias guardrail is a maintainer priority: prevent baking US health-system cultural/economic assumptions into module shapes before international evidence arrives. Batch-filed as one Discovery entry per the OPP-0008/0011 precedent; single-PR landing preserves the cross-references among the five. Distillation companion rule satisfied by construction (two substantive observations); this row satisfies the knowledge-capture + opportunity-capture audit-trail floor tripped by the OPP and shared-observations edits. | @unclenate | OPP-0018, OPP-0019, OPP-0020, OPP-0021, OPP-0022; aug OPP-0013, OPP-0016 |
| 2026-05-24 | Discovery | Filed seven new candidate OPPs (OPP-0011..0017) from the OpenEMR brownfield canonization session at `unclenate/openemr@eced4ce`. Originating evidence is the consumer-project gap analysis at `docs/knowledge/harness-coverage-gap-analysis.md` (20 numbered gaps grouped by harness artifact type: modules, templates, skills, validators, compositions). OPP-0011 (PHP stack family): `stacks/php` module + `harness-php` skill + `validate-php-strict-types.sh` + `validate-conventional-commits.sh`. OPP-0012 (SQL data engine generalization): rename `data/relational-postgres` → `data/relational-sql` with an `engine` sub-field (postgres / mysql / mariadb / sqlite), preserving 95% of required-artifact content. OPP-0013 (healthcare domain family): decomposed `domains/healthcare-*` family of 12 sub-modules (`-fhir`, `-hl7v2`, `-smart-on-fhir`, `-ccda`, `-eprescribing`, `-cdr`, `-cqm`, `-phi-encryption`, `-audit-log`, `-direct-messaging`, `-ehi-export`, `-patient-portal`) + `templates/healthcare/` (8 templates) + `harness-healthcare` skill + `healthcare-full-ehr.yaml` convenience composition. OPP-0014 (polyglot-companion-services): new module for the "one language per service surface" pattern, triggered by OpenEMR's ccdaservice Node.js companion. OPP-0015 (regulated-compliance pattern): `domains/regulated-compliance` module + compliance templates + `regulated-saas.yaml` composition, generalizing the Inferno ONC G10 external-test-kit pattern beyond healthcare. OPP-0016 (specialist healthcare review skills): refinement-layer skill family (`harness-fhir`, `harness-hl7v2`, `harness-onc-certification`, `harness-phi-audit`, `harness-encryption-review`, `harness-rbac-review`). OPP-0017 (legacy-coexistence templates + PHI tripwire validator): six templates — three coexistence (`upgrade-vault-history`, `dual-data-layer-migration`, `template-engine-migration`) plus three auth (`oauth2-with-smart-scopes`, `acl-rbac-design`, `sub-app-portal-auth`) — and the `validate-no-phi-in-logs.sh` validator. Three paired observations appended to `shared-observations.md` covering (a) the cross-consumer convergence on the stack-catalog-breadth gap (OPP-0008 from YouBase + OPP-0011 from OpenEMR within 24h), (b) the module-sizing principle "consumer-audience granularity is the load-bearing axis" surfaced by the healthcare-family decomposition pressure, (c) a second-instance confirmation of the YouBase observation "Brownfield onboarding is the harness's highest-leverage catalog-gap discovery mechanism" (lifting its confidence from medium-high to high). `candidates.md` consolidates these into the existing "Brownfield catalog coverage" cluster (added by PR #49 from the YouBase batch) as a sub-grouping "From OpenEMR (PHP healthcare EHR)", paired with the existing "From YouBase (cryptographic-identity / personal-data-store)" sub-grouping — preserving the cluster as the home for both filings rather than competing headings. Filing-time observation: the OPPs were originally drafted as OPP-0008..0014 but renumbered to OPP-0011..0017 after discovering the YouBase branch had already claimed OPP-0008..0010 earlier the same day — the renumbering pattern echoes the OPP-0006 → OPP-0007 collision recorded in PR #36. | Brownfield consumer onboardings produce comprehensive gap analyses as a byproduct of the onboarding work — the agent cannot complete its task without surfacing the gaps. The OpenEMR session's gap analysis was already sized for upstream extraction (each numbered §G section mapped 1:1 to a candidate OPP). Bulk-filing all seven at once preserves the cross-references between them (e.g., OPP-0013's healthcare family references OPP-0014's polyglot pattern for its CCDA sub-module; OPP-0016's specialist skills layer on top of OPP-0013's broad `harness-healthcare`), which would fragment if each landed as a separate PR over days. Convergence with the YouBase OPPs (now on main as of PR #49) is structurally important: two independent consumer projects hitting the same "stacks catalog breadth" pattern within 24 hours justifies coordinated PRD work across the two filings rather than parallel single-project responses. None of the seven OPPs are proposed for acceptance in this PR — they enter as `proposed` candidates competing for maintainer prioritization against OPP-0006 (trust-tier-enforcement, in flight) and the public-launch quality-audit triage branches. | @unclenate | OPP-0011, OPP-0012, OPP-0013, OPP-0014, OPP-0015, OPP-0016, OPP-0017 |
| 2026-05-24 | Discovery | Filed three new OPPs as a coherent batch from the first non-trivial external brownfield onboarding pass (YouBase, 2026-05-24): **OPP-0008 — Stack Module for Plain Node-JavaScript (and Legacy CoffeeScript)**, **OPP-0009 — Data Module for Embedded Key-Value Stores (LevelDB-class)**, and **OPP-0010 — Domain Module for Cryptographic Identity (Non-Ethereum HD Wallets, Personal Data Stores)**. All three OPPs cite the same primary evidence — the YouBase onboarding assessment's Section 2 explicitly noted, for each of the three module families (stacks, data, domains), that "no catalog module fits" while the consumer's actual stack, data layer, and identity model were all unambiguous. Initial biases: OPP-0008 favors sibling modules (`stacks/node-javascript` + `stacks/coffeescript`, both zero-required-artifact like the existing `stacks/node-typescript` and `stacks/python`); OPP-0009 favors a server-vs-browser split (`data/embedded-key-value` covering LevelDB/LMDB/RocksDB/SQLite-as-KV/Bun-KV/Deno-KV plus `data/browser-storage` covering IndexedDB/localStorage/OPFS, both zero-required-artifact like `data/document-store`); OPP-0010 favors a narrow `domains/cryptographic-identity` orthogonal to `domains/web3` (deferring the broader "personal data store" product framing to a future OPP). All three deliberately ship with zero required artifacts in v1 to keep adoption friction minimal — promotion to required-artifact gates is explicitly deferred to v2 OPPs after real consumer contact. `candidates.md` gains a new cluster "Brownfield catalog coverage" carrying the three entries with explicit framing of the brownfield-discovery pattern. SUMMARY.md gains three new OPP entries. One architectural observation captured in `shared-observations.md`: *"Brownfield onboarding is the harness's highest-leverage catalog-gap discovery mechanism"* — generalizes the pattern that the first real external consumer to be put through the `harness-onboarding` skill immediately surfaced three structural catalog holes that the auto-harness self-dogfood (a Ruby + bash + markdown project) could never have surfaced, because the self-dogfood doesn't exercise the stacks/data/domains catalog at all. Status: all three OPPs `proposed`; Confidence: high. Same-day promotion to `exploring` and PRD drafts deferred — the OPP layer is the right surface for batch-filing discovery without committing to implementation order, and the three OPPs may not all be equally urgent. | This is the first time the `harness-onboarding` skill has been pointed at a real external brownfield consumer (YouBase: a 2016-era Node + CoffeeScript identity store, abandoned by its original company, fork held by the auto-harness maintainer). The skill worked as designed — produced a clean 5-section assessment, a valid lite manifest, and identified the three catalog gaps with explicit Open Questions. Filing the gaps back into auto-harness as OPPs is the discovery loop closing: brownfield-onboarding's first real exercise produces concrete additions to auto-harness's catalog. The single change-log row covers all three OPPs (rather than three rows) per the OPP-0007 precedent of treating a coherent batch as one Discovery entry; the cluster heading in `candidates.md` provides the per-OPP discoverability. The OPPs satisfy their own newly-tripped distillation companion rule by construction: the paired observation in `shared-observations.md` is substantive (generalizing brownfield-discovery as a class-of-trigger, with three concrete instances) rather than cargo-cult. No PRD pass yet — explicit choice to let the OPP layer carry the proposals without locking in implementation order; each OPP can promote to `exploring`+PRD independently when its priority surfaces. | @unclenate | OPP-0008, OPP-0009, OPP-0010 |
| 2026-05-24 | Scope | Promoted `OPP-0007 — Canonical-Position Artifact as Harness Primitive` from `proposed` to `exploring` (per established maintainer-priority cadence). Direction set on **a new lightweight overlay module** — `management/canonical-position` depending on `project-standard` — that adds one required artifact (`docs/canonical-position.md`) plus the citation companion rule plus the ratification flow. Drafted `PRD-0007 — Canonical-Position Artifact as Harness Primitive` as the paired design. PRD specifies v1 as eight FRs: (FR-001) new module; (FR-002) canonical-position template with adaptive section structure (5 required + 4 recommended sections by project shape); (FR-003) citation companion rule firing on strategy-shaped artifact edits, satisfied by either an existing citation or by canonical-position edit in the same PR; (FR-004) ratification companion rule firing on canonical-position edits, satisfied by a `docs/reviews/REVIEW-*.md` artifact; (FR-005) new review-artifact template (closes Observation C from the municipal-brain handoff); (FR-006) operating-principles § 9 additions codifying the three positive patterns from Observation E (change-log as commit-grouping spec; companion-rules discipline under load; salvage-before-archive + ARCHIVE-INDEX); (FR-007) catalog-count assertion bumps (modules_profiles 26→27, templates 56→58); (FR-008) documentation updates (SKILL.md, SUMMARY.md, discovery-to-composition rubric, templates README). PRD takes positions on all seven OPP open questions, including: new overlay module (not extending project-standard, not full-strategic-direction); file-path-only citation at v1 (commit-SHA pinning deferred); operating-principles and canonical-position are complementary distinct surfaces; full-document versioning (not per-section); module is opt-in (not required for all project-standard consumers); greenfield + opt-in for existing projects (no required migration). Bundled into v1: Observations C (review-artifact, because ratification depends on it) + E (operating-principles additions, cheap). Deferred to follow-up OPPs that will anchor on OPP-0007: Observation A (validator opt-out staleness — needs trust-tier from PRD-0006), B (opportunity-capture backlog re-audit — needs canonical-change-detection), D (discovery-intake canonical-SHA pinning — needs intake schema update). Acceptance criteria for OPP-0007 → `accepted`: PRD-0007 Accepted + FR-001..FR-008 merged + validators green + module reachable from `harness-onboarding` skill + at least one sample-project or consumer fixture demonstrating the flow. One process observation captured in `shared-observations.md`: *"Anchor-OPP-and-satellite-observations is a stronger filing shape than disconnected OPPs"* — generalizes the constellation-filing technique from the municipal-brain handoff as durable process learning. SUMMARY.md gains PRD-0007 entry; candidates.md OPP-0007 row bumped to `(exploring; PRD-0007 in flight, drafted 2026-05-24)`. | Same-day OPP→exploring+PRD draft preserves the maintainer-priority signal (per OPP-0004 cadence pattern). PRD scope-disciplined: bundle the review-artifact (Observation C) into v1 because the ratification rule it serves depends on it; defer the three deeper compositions (A, B, D) because each has substantive scope and prerequisite work. The constellation-filing observation (anchor + satellites) generalizes from OPP-0007's case to a process technique worth promoting if a second instance confirms it. Anticipated v0.7.0 release-marker (v0.6.0 is the trust-tier-enforcement implementation from PRD-0006). | @unclenate | PRD-0007, OPP-0007 |
| 2026-05-23 | Discovery | Filed `OPP-0007 — Canonical-Position Artifact as Harness Primitive` (renumbered from in-flight OPP-0006 after PR #46 took the OPP-0006 slot for trust-tier-enforcement; original PR #36 rebased on current main and renumbered as part of this PR). Appended five sibling observations to `docs/knowledge/shared-observations.md`, all derived from a multi-day reconciliation in `bdits/municipal-brain` (commit `ff953c1`). OPP-0007 proposes the canonical-position artifact as a first-class harness primitive — a required artifact of a management profile (placement to be decided at PRD pass: new module vs. extension to `project-standard` vs. lightweight overlay), with a companion-rule contract that every strategy / product / GTM / partnership artifact cites it and that it can be revised only via a ratified update. Identified as the highest-leverage single addition the harness can make against the failure class observed in `bdits/municipal-brain` (a dozen planning documents disagreeing on the platform thesis, wedge, pricing, and funding posture — root-caused by MB-REV-003 to *"there has never been a single canonical position document"*). The five sibling observations identify dependent gaps the canonical-position artifact unlocks: (A) validator opt-out has no staleness pressure; (B) opportunity-capture has no backlog-reconciliation trigger when canonical direction changes; (C) no formal review/reconciliation artifact type — the ad-hoc one proved high-value; (D) discovery-intake treats intake as one-shot; canonical-direction-changed → intake-stale path is missing; (E) three positive patterns from heavy-load reconciliation worth promoting to harness conventions — change-log as commit-grouping spec, companion-rules discipline holding under load, salvage-before-archive + ARCHIVE-INDEX. `candidates.md` updated with new cluster "Canonical direction & strategic alignment" carrying OPP-0007. SUMMARY.md gains OPP-0007 entry. The PR satisfies the cycle-end distillation companion rule by construction. | PR #36 was filed 2026-05-23 early UTC by the maintainer in parallel with the audit/Wave-1-through-3 trajectory but using OPP-0006 — the slot subsequently taken by PR #46 (trust-tier-enforcement). The substance of PR #36 is high-value field evidence (the canonical-position artifact gap is a genuine architectural concern I missed in my audit; the five sibling observations connect to multiple in-flight gaps including the trust-tier validator overrides and opportunity-capture). Rather than lose the work, the PR was rebased onto current main and renumbered OPP-0006 → OPP-0007, then merged into the session's trajectory. Sequencing-mistake observation worth noting: when filing new OPPs, check for in-flight PRs with conflicting numbers before assigning a number. | @unclenate | OPP-0007 |
| 2026-05-23 | Documentation | Wave 3-D — three new architecture diagrams added to `docs/architecture/diagrams.md`, diagram count bumped 6→9. The new diagrams cover patterns identified in the recent observation cycle as not yet visualized: **Diagram 7 — Paired Mechanism Dynamic** (showing how two pieces of machinery encoding the same concern catch each other's bugs; covers the PR #34 hook+rule case, PR #38 templates+validator case, PR #41 validator+own-state case as instances of one pattern; includes specialization "machinery that asserts against state-including-itself"). **Diagram 8 — OPP → PRD Design-Pressure Cascade** (shows the *epistemic* transitions distinct from Diagram 4's status transitions — what each document-pass forces commitment to that prior passes elided; confirmed across OPP-0004→PRD, OPP-0005→PRD, OPP-0006→PRD cycles). **Diagram 9 — Catalog-Counts Assertion Flow** (visualizes how `validate-catalog-counts.sh` works: canonical recipes → assertion table → extract → normalize → compare → pass/fail; documents the design choices "recipes inline" and "assertions inline" per operating-principles § 8). All three diagrams use the same Mermaid + pass/fail color-coding convention as the original six; intro table at top updated from "Six diagrams" to "Nine diagrams". Back cover SVG `diagrams` count bumped 6→9 with descriptive list expanded ("paired-mechanism · OPP/PRD pressure · catalog-counts" appended). `validate-catalog-counts.sh` caught the 6→9 drift on first run after the diagrams were added (the cover-back.svg assertion site) and exited 1 until the cover was updated — exact same self-stabilization dynamic the diagram 7 itself describes. | The 2026-05-23 audit + the diagrams-refresh request explicitly named: (a) several patterns from recent observations not currently diagrammed (self-stabilization, OPP→PRD design-pressure cascade, paired-mechanism dynamic, catalog-counts validator's assertion-table flow); (b) need for the diagrams document to stay current as Wave 2/3 work added artifacts. This PR addresses both. Trust-tier-enforcement diagram (item #4 from the request) deferred until PRD-0006 implementation lands (the diagram would visualize what doesn't exist yet); Wave-2-docs-process flowcharts (item #5) and dedicated catalog-count sub-flow diagram (item #6 in the request — implemented as Diagram 9) covered here. Self-stabilization pattern (item #1) and paired-mechanism dynamic (item #3) collapsed into one diagram (Diagram 7) since they're a parent-child shape. Diagram 8 is the OPP→PRD cascade (item #2). Three new diagrams gives substantial coverage of the patterns from the recent session's observation cycle without ballooning to one-per-pattern. | @unclenate | — |
| 2026-05-23 | Scope | Filed `OPP-0006 — Trust-Tier Enforcement (Doctrine → Machinery)` and promoted `proposed → exploring` same-day per established cadence. Drafted `PRD-0006 — Trust-Tier Enforcement — Making Doctrine Machine-Checkable` as the paired design. Closes the highest-priority gap from the 2026-05-23 audit and the *"Doctrine in prose without enforcement in code is a recurring harness gap pattern"* observation. OPP enumerates five design options (A-E); initial bias **Option E (Hybrid)** — optional `tier` field on `module.yaml` + `sensitivePaths` pattern inference + new `validate-trust-tier.sh` validator + dogfood declarations on auto-harness's own modules. Backward compatibility preserved (additive schema; legacy modules pass via inference). PRD takes positions on all six OPP open questions: module-level tier (not per-rule), optional with inference fallback (not required), both inference + declaration with declaration winning, no transitive propagation in v1, PR-boundary enforcement only (not session-level), PR template checkboxes kept manual in v1. PRD specifies seven FRs: schema, inference table, validator script, kernel/CI/skill wiring, dogfood declarations on active modules, documentation updates (trust-model.md "Enforcement" section, threat-model.md A5 mitigation move), catalog-counts assertion bump (validators 8→9). Explicit non-goals: session-level enforcement, required `tier` field, PR-template auto-fill, tier-tagged commits, transitive propagation, cross-client allowlist reconciliation, per-rule tier. Acceptance criteria for OPP-0006 → `accepted`: PRD Accepted + FR-001..FR-007 merged + self-dogfood + CI green + threat-model.md A5 mitigation list updated. One paired architectural observation captured in `shared-observations.md`: *"Inference-with-declaration-override is the right shape for opt-in governance enforcement"* — generalizes the design shape across three instances (catalog-counts, header hygiene, trust-tier) and proposes applying it to remaining audit findings (knowledge curation, consumer module operations). Implementation deferred to a follow-up PR (the v0.6.0 release-marker). | OPP-0006 is the most strategically important Wave 3 item per the audit's framing. Same-day OPP→exploring+PRD draft preserves maintainer-priority signal (per OPP-0004 cadence pattern); implementation deferred for proper alignment given the scope (multiple FRs, schema change, new validator, dogfood pass across all active modules, documentation updates). PRD scope-disciplined: opt-in additive schema (avoids breaking change), inference as fallback coverage, PR-boundary enforcement (session-level deferred). The observation captures a pattern that's now visible across three independent design decisions and recommends applying it to the remaining audit findings. | @unclenate | PRD-0006, OPP-0006 |
| 2026-05-23 | Governance | Wave 3-B of audit trajectory — three small deliverables addressing quality + ergonomics gaps. (1) **Sample-project CI validation**: new `sample-projects` job in `.github/workflows/harness.yml` runs `validate-manifest` + `validate-module-graph` + `validate-required-artifacts` + `validate-agent-pack` against each of the 5 samples on every PR / push. Closes the "samples have no validation CI" finding from the 2026-05-23 audit. Placeholders + companion-rules deliberately skipped per documented rationale (samples include pedagogical token mentions; companions is PR-diff-based and meaningless against static samples). Two samples (`node-web-saas-postgres`, `submodule-consumer`) were missing `docs/project/revision-tracker.md`; created from template with filled illustrative findings. (2) **Consumer CI templates**: new `platform/templates/ci/` directory with `github-actions.yml`, `gitlab-ci.yml`, and a `README.md` documenting the templates' contract, customization points, and the companion-rule caveat. Templates use tokenized headers so they ship through the existing `set-consumer-headers.sh` flow. Closes the "no CI templates for consumers" finding. (3) **Knowledge query helper**: new `platform/bootstrap/query-observations.sh` — pure-bash + ripgrep filter for `docs/knowledge/shared-observations.md` by severity / topic / date / full-vs-compact view. Closes the v1 of the "knowledge management is write-only" gap (per the 2026-05-23 audit's #3 critical finding; the deeper curation workflow remains future work). Self-test: running `query-observations.sh --severity=architectural` returns 11 entries cleanly. Both new scripts are Bash 3.2 compatible and shellcheck-clean. SUMMARY.md gains two new sections under Validator Reference: Bootstrap Tools (registers all 5 bootstrap scripts including the new query helper) and CI Templates (registers the new consumer-facing CI starter pack). | All three deliverables were identified by the 2026-05-23 audit as small/quality-of-life Wave 3 items. Bundled per established preference. The catalog-counts validator from PR #41 was again the load-bearing safety net — adding `query-observations.sh` as a new bootstrap script doesn't change the validator-script count, so no count drift; but the CI workflow file gained a new step, which is unvalidated text but reviewed manually. Sample-CI deliberately scoped narrower than the harness-self-validation chain to ship in this PR without expanding to a full sample-cleanup pass; comprehensive sample placeholder cleanup is deferred as separate work. | @unclenate | — |
| 2026-05-23 | Release | First tagged release: `v0.5.0`. CHANGELOG.md restructured — the long-standing `[Unreleased]` section's accumulated content (post-2026-05-18 work through Wave 2 of the audit trajectory) consolidated into a new `## [v0.5.0] — 2026-05-23` section structured per the [Keep a Changelog](https://keepachangelog.com/) convention (Added / Changed / Fixed / Architectural observations / Known limitations / Upgrade notes). `[Unreleased]` is now empty at the top of the file. Cover SVGs (`docs/_assets/cover-front.svg`, `cover-back.svg`) bumped from placeholder `v1.0` to actual `v0.5`. Git tag `v0.5.0` to be created on the merge commit per the release operational process documented in `platform/workflow/release-and-versioning.md` (PR #43, Wave 2). GitHub release also follows. | First-tag is foundational: every Wave 3 deliverable downstream (trust-tier enforcement, knowledge tooling, sample CI) needs a versioned target. The release-and-versioning.md policy from Wave 2 defined the semver interpretation; this PR exercises it for the first time. v0.5.0 chosen (not v0.1.0 / v1.0.0) because: (a) the project has accumulated substantial machinery and is materially more than v0.1; (b) the trust-tier enforcement gap (audit's #1 critical finding) is a deliberate scope item before v1.0, so we are firmly pre-1.0; (c) v0.5.0 leaves room for v0.6/v0.7/v0.8/v0.9 incremental releases on the path to v1.0 without immediate pressure to claim 1.0 prematurely. No breaking changes; consumers pinned to commit hashes can move to `v0.5.0` cleanly per the upgrade-notes section. | @unclenate | — |
| 2026-05-23 | Documentation | Wave 2 of audit-driven documentation expansion. Five new authoritative docs filling workflow coverage gaps identified by the 2026-05-23 audit: (1) `platform/workflow/extending-the-harness.md` — module / validator / skill / template / agent-pack author guide for contributors and consumer-organizations extending the harness; explains where each lives, what schemas they follow, what catalog updates they trigger. (2) `platform/workflow/modify-composition-mid-project.md` — operational guide for adding / removing / changing modules in an active manifest, including a sunset section for projects ending harness adoption. Closes the gap where bootstrap covered initial adoption and maintenance covered upstream pulls but nothing covered re-composition. (3) `platform/workflow/incident-response.md` — five-phase operational workflow (stabilize → investigate → document → distill → close) using the existing `incident.md` template; codifies blameless postmortem discipline and the distillation bridge from incidents to operating-principles. (4) `platform/workflow/release-and-versioning.md` — semver policy specific to the harness, pre-1.0 rules, release operational process (CHANGELOG.md structure, git tag procedure, GitHub release flow), deprecation policy, yank semantics, sub-artifact versioning. Sets up Wave 3 work (first version tag) by defining the policy first. (5) `docs/threat-model.md` — companion to SECURITY.md covering asset inventory, five adversary models (curious consumer, malicious contributor, compromised maintainer, adversarial regex input, compromised AI agent), specific attack surfaces (workflow injection, template content, validator scripts, submodule attacks), mitigations in place, and mitigations not yet deployed (with cross-link to OPP candidates). SUMMARY.md TOC reorganized: Day-to-Day Workflows gains cycle-end-distillation + modify-composition + incident-response + multi-agent-tool-coordination rows; Maintenance & Operations gains release-and-versioning; new "Contributing & Extension" section added with extending-the-harness + threat-model. Workflow count bumped 14→18 across all 4 documented assertion sites (caught immediately by `validate-catalog-counts.sh`, fixed in same PR). | Wave 2 of the 2026-05-23 audit trajectory. The audit identified five workflow / capability gaps that were documentation-shaped (vs. machinery-shaped, which is Wave 3). Each new doc closes a specific gap: extending-the-harness for contributor onboarding; modify-composition for the missing operational case between initial adoption and upstream maintenance; incident-response for the gap between SECURITY.md (disclosure) and operational reality; release-and-versioning for the *no-versions-exist* finding; threat-model for the *disclosure-without-threat-modeling* finding. Bundled as one PR per established preference. The catalog-counts validator from PR #41 was the load-bearing safety net: it caught the workflow count drift caused by adding three new docs to `platform/workflow/`, preventing the same class of bug the validator was built to close. | @unclenate | — |
| 2026-05-23 | Documentation | Wave 1 of audit-driven documentation drift closure. (1) `SUMMARY.md` gains nine previously-missing governance artifacts (ADR-0009/0010/0011/0012, PRD-0001/0002/0004/0005, OPP-0004/0005) plus a new "Product Requirements Documents" section between ADR and Opportunity sections; the candidates index also gets a TOC entry. All recent work (PRs #29-#41) is now discoverable from GitBook's table of contents. (2) `README.md` validator count corrected at three call sites (line 97 "seven shell scripts" → "eight"; line 348 "Seven validators" → "Eight"; the validators table gains a new row for `validate-catalog-counts.sh`). (3) `README.md` Reference section gains an entry for `docs/architecture/diagrams.md` with a one-line description naming the six diagrams — closes the gap where the architecture diagrams were reachable only from SUMMARY.md, not from the canonical README entry-point. (4) `validate-catalog-counts.sh` extended with `normalize_count()` for English-number-word forms (one through twenty) plus four new assertions covering README prose word-form claims and the skills-and-agents.md word-form claim. Validator now passes 23 assertions (was 19). One paired observation captured: "Doctrine in prose without enforcement in code is a recurring harness gap pattern" — generalizes the audit's finding across trust-tier enforcement, versioning, knowledge curation, and consumer module operations into a single durable insight. | Quality + capability audit (2026-05-23, this session) identified the drift; this PR closes the documentation drift portion in one bundled commit. Audit pattern: two parallel exploration agents enumerated documentation gaps and missing capabilities; both agents independently surfaced the SUMMARY.md missing-entries issue and the README validator-count drift. Wave 1 is the small/immediate set (~30 min) that unblocks reader discoverability of recent work; Waves 2 and 3 (workflow docs, foundational machinery) follow in subsequent PRs. The observation paired with this PR isn't strictly required by any rule (no trigger paths in diff), but the audit produced a durable architectural insight worth capturing per the workflow doc's recommendation to distill substantive learning even outside trigger boundaries. | @unclenate | — |
| 2026-05-23 | Governance | New `validate-catalog-counts.sh` validator + wired into kernel + CI. Closes the drift class identified in `shared-observations.md` (2026-05-22): *"Each new artifact asserting a catalog count is a new place that fact can drift."* The validator runs inline recipes (`find platform/profiles -name module.yaml \| wc -l`, etc.) to compute canonical counts for seven metrics (modules_profiles, modules_all, validators, skills, templates, workflows, diagrams), then iterates a 19-row assertion table mapping `(file, regex, count-key)` triples to documented claim sites across `platform/reference/how-to-read.md`, `docs/architecture/diagrams.md`, and `docs/_assets/cover-back.svg`. Each documented claim's number is extracted and compared to canonical; drift produces a clear `file: claims N but canonical is M` error and exits 1. Adding the validator bumped `validators` count 7→8, which the validator's own first run caught at four call sites (entry-point prose, ASCII art, diagram-1 label, back cover) — proving the self-stabilization claim by construction. Wired into `kernel/base` module.yaml validators list, into the CI workflow as a new "Validate catalog counts" step between doc-references and companions, and documented in the `harness-governance` SKILL validator chain + signature-notes section. When a new doc cites a catalog count, append a row to the script's `ASSERTIONS` table to keep the drift class closed. | Captured in `shared-observations.md` (PR #39) as a recommendation; OPP-0005's "two classes of token" lesson generalized: text-representation artifacts asserting the same fact must be kept in sync or they decay. Pure structural fix — no human discipline required at PR time once landed. Self-stabilization confirmed (this PR's own validator-count bump exercised the validator on its first run, which caught the doc drift the bump itself created — same dynamic as PR #34's hook adapter catching the rule's regex-scope bug). | @unclenate | — |
| 2026-05-23 | Governance | Promoted "Prefer Text Representations" to `docs/operating-principles.md` as new § 8. The principle codifies an existing pattern in the harness — YAML modules, Bash validators, Markdown SKILL files, Mermaid diagrams, SVG covers, etc. all chose text over binary on independent design pressure; § 8 names the pattern as durable doctrine. Section enumerates the five reasons (versionable, toolchain-free, CI-friendly, diffable, future-proof), an applied-vs-rejected table covering eight surfaces, three legitimate exception classes (photography/illustration; downstream render targets; round-trip-required binaries), and the test question for new artifacts: *"can this be expressed as text without losing essential meaning?"* Header `Last updated` bumped to reflect the § 8 addition. | The pattern accumulated enough instances (YAML manifests + Bash validators + Markdown docs + Mermaid diagrams + SVG covers) across enough independent design decisions that it qualifies as a durable how-this-project-works truth per the operating-principles bar ("true regardless of who is doing the work, what they are working on, and when"). Naming it explicitly turns an implicit convention into a citable design constraint — future contributors and consumers can point at § 8 when arguing against a binary artifact addition. Cheap-satisfier discipline (ADR-0010) applies: no new ADR/PRD spawned because this is synthesis-of-existing-pattern rather than a new design decision; change-log entry is the appropriate weight. | @unclenate | — |
| 2026-05-22 | Documentation | Documentation visualization + QA pass. (1) New `docs/architecture/diagrams.md` central hub with six Mermaid diagrams covering composition, trust-tier decision flow, companion-rule firing (trigger → satisfier → CI gate), opportunity-to-decision lifecycle, distillation-trigger composition (active hook + passive rule), and consumer adoption end-to-end. Each diagram has prose framing, color-coded states (pass/fail/blocked/escalation), and a "how it composes with the others" section at the bottom. (2) Six targeted embeds: HARNESS.md (overview pointer), `harness-governance` SKILL.md (trust-tier + companion-rule callouts), `cycle-end-distillation.md` (distillation diagram link), `opportunity-capture/README.md` (lifecycle diagram link), `submodule-integration.md` (consumer-adoption diagram link), SUMMARY.md (TOC entry in Concepts and Reference section). (3) QA pass fixing stale catalog counts in `platform/reference/how-to-read.md`: `55 templates → 56` in prose line; `(55 files) → (56 files)` and `(13 guides) → (14 guides)` in the authority-stack ASCII art. Module count `26` confirmed correct per the documented recipe (profile-only count, excluding core/agents). (4) Two cover SVGs at `docs/_assets/cover-front.svg` and `cover-back.svg` for GitBook PDF / print export — 1600×2400 (2:3) bold-typography + nested-rectangle composition motif; cream background, deep-slate accent; cleanly rescales to US Letter / A4 / trade. Self-documenting `docs/_assets/README.md` covers the palette, style intent, GitBook integration notes, and the SVG-over-PNG rationale. (5) One paired architectural observation: "Each new artifact asserting a catalog count is a new place that fact can drift" — captures the maintenance cost added by the new diagrams + back cover both restating the same catalog counts; recommends an eventual `validate-catalog-counts.sh` validator while documenting the human-discipline workaround in the meantime. | Visual + cross-reference communication asked for to support team-member onboarding, repo-user comprehension, and GitBook PDF/print exports. Six diagrams chosen to cover the system's six conceptually distinct slices (composition / authority / enforcement / lifecycle / distillation / adoption) — fewer would have left gaps, more would have produced redundancy. Mermaid chosen over external SVG/PNG because it's text-based (versionable, diffable, no toolchain), GitBook renders it natively, and editing happens in the same file as the prose context. SVG covers chosen over raster (PNG/JPG) for the same versionability + scale-without-pixellation reasons; rsvg-convert instructions documented for downstream tools that need raster. QA pass deliberately minimal — only true stale-count claims fixed, no retroactive edits to historical ADRs/PRDs/release-intent records (those are temporal snapshots, not auto-updating catalogs). | @unclenate | — |
| 2026-05-22 | Governance | PRD-0005 v1 fully shipped + OPP-0005 promoted `exploring` → `accepted`. Five FRs landed: (FR-001) 61 template files under `platform/templates/**` had their literal SPDX/copyright headers replaced with token forms (`YEAR`, `OWNER_NAME`, `OWNER_EMAIL`, `SPDX_LICENSE` in `[[…]]` brackets); the `Part of auto-harness` provenance line dropped (consumer copies shouldn't claim derivation). All 61 templates had byte-identical headers pre-change so the transformation was a single mechanical pass. (FR-002) New `platform/bootstrap/set-consumer-headers.sh` (250 lines, pure bash, shellcheck-clean) — interactive prompts by default with TTY auto-detection; flag-driven for CI / scripted use; reads / writes `.harness-headers.yaml` project-local config so subsequent template scaffolds auto-fill; supports `--dry-run`, `--files=path1,...`, `--scan=DIR`; portable BSD/GNU sed in-place edit with backup-suffix pattern; basic email-shape validation. (FR-003) 78 sample-project files (70 markdown, 5 yaml, 3 shell) gained a leading re-attribution marker comment block (per-extension comment style — HTML for md, `#` for sh/yaml) explaining that derivative copies must re-attribute via the bootstrap helper. Existing SPDX block preserved (sample-projects are worked examples authored by the maintainer). (FR-004) Two-occurrence attribution drift in `platform/bootstrap/add-license-headers.sh` fixed (line 2 header + line 64 `AUTHOR=` variable — one more than the OPP flagged; the audit's original recommendation 2026-05-18 to add a CI guard remains future work). (FR-005) `platform/bootstrap/README.md` gained "Setting consumer project headers" section + new tool-table row + cross-link to the helper; `platform/templates/README.md` gained the "two classes of token" callout documenting the project-wide-vs-per-record distinction the bootstrap helper enforces. Architectural observation captured: "Header-token classes split cleanly along project-wide vs. per-record axis" — generalizes the design pressure from PRD-0005 v1 to future tokenized surfaces. PRD-0005 status Proposed → Accepted; OPP-0005 status exploring → accepted; candidates.md row reflects full v1 shipped. | OPP-0005 acceptance criteria 1-4 all met — PRD Accepted, FRs all merged, auto-harness self-dogfoods (the 61 tokenized templates pass validation via existing `platform/**` exemption in `.placeholder-ignore`), end-to-end smoke test verified (template → cp → `set-consumer-headers.sh` → filled output). v1 deliberately scoped to forward-fix templates and ship the bootstrap helper; consumer-migration tool for already-inherited bad headers deferred to follow-up per PRD § Future Work, with manual remediation pattern documented in the v1 PR. PRD-design pressure surfaced the project-wide-vs-per-record token-class distinction that wasn't visible at OPP time — captured as durable architectural learning. Closes OPP-0005 v1. | @unclenate | PRD-0005, OPP-0005 |
| 2026-05-22 | Scope | Promoted `OPP-0005 — Consumer Header Hygiene` from `proposed` to `exploring` (same-day flip after filing) with Disposition populated: direction set on **Option A + Option B** from the four candidates — tokenize template headers so the existing `validate-placeholders.sh` machinery gates new files, plus a small consumer-facing bootstrap helper to fill tokens project-wide. Option C (full `management/header-hygiene` module) deferred as premature primitive-creation; Option D (strip headers entirely) declined because templates lose pedagogical value. Sample-projects retain attribution but gain re-attribution markers. Drafted `PRD-0005 — Consumer Header Hygiene — Stop Template Attribution Leakage` as the paired design. PRD specifies v1 as five coordinated pieces: (FR-001) tokenize every header block under `platform/templates/**` with uppercase token forms that match the existing placeholder validator's regex; (FR-002) ship `platform/bootstrap/set-consumer-headers.sh` — interactive + flag-driven helper that writes a project-local config (`.harness-headers.yaml`) and substitutes tokens across template-derived files; (FR-003) add re-attribution leading-comment markers to every sample-project file (sample-projects are worked examples, not scaffolding sources); (FR-004) fix one-line internal drift in `platform/bootstrap/add-license-headers.sh` line 2 (work email → personal email per the project's own attribution rule); (FR-005) document the consumer onboarding flow in `platform/bootstrap/README.md` and add a tokenization note to `platform/templates/README.md`. PRD takes positions on all six OPP-0005 open questions, including: tokenize SPDX (not just copyright + email) because consumer licensing is genuinely their choice; config-file over per-prompt because lower friction and regeneratable; script in `platform/bootstrap/` rather than a new module because header hygiene is a consumer-onboarding concern not a governance-policy concern. Explicit non-goals: consumer-migration tool for already-inherited bad headers (follow-up); `validate-headers.sh` post-hoc detector (token-validator gate at template-creation is the v1 floor); language coverage beyond the existing extension list. Acceptance criteria for OPP-0005 → `accepted`: PRD-0005 Accepted + FR-001..FR-005 merged + auto-harness self-dogfoods + one verified consumer / fixture bootstrap. Bumped OPP-0005 candidates.md entry `(proposed)` → `(exploring; PRD-0005 in flight, drafted 2026-05-22)`. One process observation captured in `shared-observations.md`: "PRD drafts surface questions the originating OPP successfully elided — the OPP→PRD pipeline is a discipline, not a redundancy" — generalizing the pattern that document pairs apply different kinds of design pressure (sibling to PRD-0004's paired-mechanism finding, lifted from validator pairs to document pairs). | Same-day promotion preserves the maintainer-priority signal (per OPP-0004's "Maintainer 'I thought that was already happening' is the highest-signal gap-discovery pattern") and provides the structured surface to drive the implementation PR. PRD scope-disciplined to ride existing machinery wherever possible (cheap-satisfier discipline from ADR-0010 + file-boundary-as-precision from ADR-0012 both informing the design); deliberately resists module-creation and validator-creation in favor of composing primitives already in-tree. PRD design pressure surfaced three OPP-elided decisions (tokenize SPDX; config-driven; no new module) that only had defensible answers once the implementation surface was specified — observation captures that dynamic as durable process learning. | @unclenate | PRD-0005, OPP-0005 |
| 2026-05-22 | Discovery | Filed `OPP-0005 — Consumer Header Hygiene` capturing a legal-correctness gap surfaced by the maintainer in conversation post-PR-#34: 61 template files in `platform/templates/**` and every sample-project file ship with literal auto-harness SPDX/copyright headers (the maintainer's name and email, dual MIT/Apache-2.0), so consumers who scaffold ADRs/PRDs/observations from templates inherit the wrong attribution and license. Asymmetric tooling confirmed: `platform/bootstrap/add-license-headers.sh` operates only on auto-harness's own tree (no consumer equivalent); `validate-placeholders.sh` already exists and would gate `[[…]]`-style fills if templates were tokenized (machinery present but not composed with templates). OPP enumerates four design options (A: tokenize templates only; B: tokenize + bootstrap helper; C: full `management/header-hygiene` module; D: strip headers from templates entirely) with explicit tradeoffs. Initial bias: A+B (cheapest viable v1 leveraging existing placeholder validator, plus a small consumer-facing bootstrap helper). Side-finding: `add-license-headers.sh` line 2 attributes itself to the wrong email (work, not personal) — internal drift to fix in the same implementation pass. Added new `candidates.md` cluster "Consumer onboarding & project hygiene" + OPP-0005 row; bumped OPP-0004 row to reflect full v1 shipped. Status: proposed; Confidence: high. One architectural observation captured in `shared-observations.md`: "Harness primitives that don't compose toward the consumer-side surface are silent governance gaps" — generalizing the diagnosis pattern (primitive A + primitive B both exist; consumer-side surface C needs A∘B; nothing composes them) for future audit passes. | Maintainer-noticed legal correctness issue (per OPP-0004's "Maintainer 'I thought that was already happening' is the highest-signal gap-discovery pattern"). Filing as OPP now (rather than implementing directly) preserves the priority signal, captures the four candidate designs with tradeoffs for PRD-pass alignment, and surfaces the meta-pattern (asymmetric composition of existing primitives) as durable architectural learning. This is the first OPP filed *after* the distillation-trigger rule went live; the rule fires (OPP path matches) and is satisfied (paired observation written) — demonstrating the PR #33+#34 machinery working on its first real cycle outside its own introducing PRs. | @unclenate | OPP-0005 |
| 2026-05-22 | Governance | `agents/claude-code` v1.0.0 → v1.1.0: shipped PRD-0004 FR-006/FR-007 — the Claude Code `Stop`-event hook adapter that emits a structured cycle-end distillation prompt in-session when the current branch carries distillation-worthy work (new ADR / OPP / module manifest / catalog change) and no knowledge destination has been touched. (1) New reference implementation at `platform/examples/sample-projects/node-web-saas-postgres/.claude/hooks/distillation-prompt.sh` — silent-exit fast-paths for non-git, detached-HEAD, on-base-branch, no-base-ref, no-diff, no-trigger, and already-satisfied cases; logs each firing to `.claude/logs/distillation.log` for audit traceability; honors `HARNESS_BASE_BRANCH` env override for non-`main` projects. (2) `agents/claude-code/module.yaml` lists the script under `optionalArtifacts`; `README.md` carries an install snippet for `.claude/settings.json` Stop-hook registration. (3) `.gitignore` gains a scoped negation (`!platform/examples/sample-projects/*/.claude/`) so checked-in reference implementations under sample-projects ship alongside the consumer-runtime `.claude/` ignore — the boundary the ignore was doing precision-work for is preserved, but tracked reference code no longer collides with it. (4) Workflow doc `platform/workflow/cycle-end-distillation.md` § How Agents Should Use This cross-links the hook at step 2 ("in-session reminder; rule is the floor"); table row updated. (5) PRD-0004 status `Accepted` (v1 functional requirements all shipped); OPP-0004 Disposition extended noting follow-up landed. (6) Bug fix during implementation: companion rule's trigger regex broadened from `^platform/profiles/.+/module\.yaml$` to `^platform/.+/module\.yaml$` so the rule covers the 8 agent-pack modules under `platform/agents/*` and the kernel module under `platform/core/kernel/base/`, not just profile modules. Applied in four places (rule's triggerPaths, hook script's grep regex + comment block, workflow doc table, skill table). (7) One fresh architectural observation captured in `shared-observations.md` — "Paired-mechanism implementation is a free correctness check on the governance side of the pair" — codifying the bug-discovery dynamic (writing the second mirror of a regex pattern forces re-derivation that catches silent-narrowness in the first). | The follow-up PR PRD-0004 anticipated. The hook adapter is the *active* layer; the companion rule shipped in PR #33 is the *passive* PR-boundary floor. Both fire on the same change classes; the hook prompts in-session before the rule trips in CI. Side benefit not anticipated: the implementation pass surfaced and fixed a real scope bug in the rule itself, because writing the hook's trigger-detection logic forced enumeration of every place a module manifest lives under `platform/`. Paired mechanisms are a free correctness check on each other — a sibling discovery dynamic to property-based testing applied to governance regex. Closes OPP-0004 follow-up scope; the v1 trigger machinery (validator + hook) is now fully shipped. | @unclenate | PRD-0004, OPP-0004 |
| 2026-05-22 | Governance | `management/knowledge-capture` v1.0.0 → v1.1.0: shipped the v1 cycle-end distillation trigger machinery per PRD-0004. (1) Added a fourth companion rule to `platform/profiles/management/knowledge-capture/module.yaml` — triggers on new/modified ADR (`docs/adr/ADR-*`), OPP (`docs/opportunities/OPP-*`), module manifest (`platform/profiles/*/module.yaml`), or active-module catalog (`harness.manifest.yaml`); satisfiers are any of the three knowledge destinations (`shared-observations.md`, `operating-principles.md`, `distilled-learnings.md`); `humanReview` text demands substantive connection between trigger work and distillation entry (no cargo-cult observations). Rides existing `validate-companions.sh` machinery — no new validator code. (2) New canonical workflow doc `platform/workflow/cycle-end-distillation.md` defines cycle-end signals, the satisfier decision tree, composition with existing audit-trail rules, anti-patterns (cargo-cult observations, distillation-fatigue avoidance, pre-emptive bypass via overrides, auto-generated entries), and how agents + humans should use the pattern. (3) Heartbeat prose in `knowledge-capture/README.md` § Agent Behavior step 5 and `opportunity-capture/README.md` § Agent Behavior step 5 cross-links the new workflow doc, replacing aspirational documentation-only references with actionable contract. (4) `harness-governance` skill SKILL.md gains a new row in the common-companion-rules table + a callout block referencing the workflow doc. (5) `docs/operating-principles.md` § 3 gains a new bullet codifying the trigger-side-vs-destination-side rule duality (Last updated bumped 2026-05-20 → 2026-05-22). (6) OPP-0004 status `exploring` → `accepted`; PRD-0004 status `Proposed` → `Accepted`; candidates.md entry annotated to reflect acceptance. (7) Implementation pass produced one new architectural observation in `shared-observations.md` — "Distillation triggers can land without bootstrap exception when the introducing PR carries genuine learning" — captured naturally as the PR satisfied its own new rule by construction without using `overrides.disabledValidations`. | PRD-0004's v1 minimum-viable trigger machinery shipped in a single coherent PR following the cadence: PRD draft → review → implementation. Achieves OPP-0004 acceptance criteria 1-4 (PRD Accepted; rule landed; dogfooded firing; OPP flipped to accepted). The active-hook adapter (FR-006/007) is should-have follow-up work tracked under the same PRD, may land in a second PR. PR satisfies its own new rule by construction — observed and captured as a fresh meta-observation about valuable rule introductions being self-stabilizing. | @unclenate | PRD-0004, OPP-0004 |
| 2026-05-22 | Discovery | Manual distillation pass on PR #32 (the PRD-0004 draft branch) capturing two architectural/process observations *as proof that the maintainer can do manually what the system PRD-0004 specifies will soon automate*. (1) "Companion-rule machinery was already most of the gap-closer for distillation" (architectural; verified by mapping PRD-0004's pseudocode against existing `validate-companions.sh` fields with no engine change required — the harness's existing primitives reach further than current docs claim). (2) "Maintainer 'I thought that was already happening' is the highest-signal gap-discovery pattern" (process; codifies what made OPP-0004 itself a P0 — assumed-features-as-gaps are qualitatively different from audit-discovered drift). Synced shared-observations.md `Last Updated` to 2026-05-22. | The user explicitly requested a distillation pass before merging PR #32, framing it as "run a distillation and then merge — commence the suggested cadence." Beautiful inversion: distilling BEFORE the trigger machinery lands proves the cycle the trigger will soon automate. Both observations are durable architectural truths derived from this PRD-design cycle, not session-specific artifacts. The first observation argues for "audit existing primitives before building new machinery" as a recurring discipline; the second proposes "assumed-feature-as-gap mining" as a gap-discovery technique. | @unclenate | OPP-0004, PRD-0004 |
| 2026-05-21 | Scope | Promoted `OPP-0004 — Distillation Triggers` from `proposed` to `exploring` (same-day flip after filing); Disposition populated with current investigation direction (hybrid passive + active trigger layer scoped as additions to `management/knowledge-capture` + `agents/claude-code`, heartbeat prose to be formalized into workflow rather than retired). Drafted `PRD-0004 — Distillation Triggers — Cycle-End Trigger Machinery for Institutional Knowledge` as the paired design. PRD specifies v1 as three coordinated pieces: (1) new companion rule on `management/knowledge-capture/module.yaml` declaring distillation-worthy signals (new ADR, new OPP, OPP `proposed` flip, new module manifest, harness.manifest.yaml change) as triggers and the three knowledge destinations as satisfiers — rides existing `validate-companions.sh` machinery, no new validator code; (2) Claude Code `Stop`/`SessionEnd` hook adapter under `platform/agents/claude-code/` emitting a structured distillation prompt from session commit pattern; (3) new workflow doc `platform/workflow/cycle-end-distillation.md` codifying cycle-end signals + satisfier decision tree + composition with existing rules + anti-patterns. Explicit non-goals: Cursor/Copilot/Codex adapters, commit-message scanning, auto-generated entries, threshold-batched distillation, new `management/distillation-cadence` module, retirement of the scheduled-review cadence. OPP-0004 candidates.md entry annotated `(exploring; PRD-0004 in flight)`. | Maintainer-stated core-function gap flagged immediately post-PR #30 merge. Same-day OPP filing + promotion + PRD draft preserves the priority signal and provides the structured surface to drive the implementation PR. PRD takes positions on all six open questions from OPP-0004 with explicit "v1 will validate" criteria, scope-disciplined to ride existing machinery wherever possible (ADR-0010 cheap-satisfier discipline + ADR-0012 file-boundary-as-precision both informing the design). Acceptance criteria for OPP-0004 → `accepted` defined: PRD lands as `Accepted` + companion rule lands in knowledge-capture + auto-harness dogfoods with one real distillation-trail entry. | @unclenate | PRD-0004, OPP-0004 |
| 2026-05-21 | Discovery | Filed `OPP-0004 — Distillation Triggers (Closing the Cycle-End Gap)` capturing a core-function gap surfaced by the maintainer immediately following PR #30: auto-harness provides the *destinations* for distilled knowledge (shared-observations, distilled-learnings, operating-principles, ADRs) but provides no *triggers* that reliably cause distillation to happen during or after a work cycle. The harness's "heartbeat with Knowledge Contribution step" prose is aspirational documentation, not actionable machinery — no skill, validator, or hook enforces it. The recently-merged observation about scheduled-review cadence going untriggered (PR #30) addressed *what's replacing* the cadence (consumer-driven issues); this OPP addresses *why the cadence didn't fire in the first place* and what trigger layer would. Status: proposed; Confidence: high. Added to `docs/opportunities/candidates.md` under new cluster "Knowledge distillation & self-improvement loop." Promotion to `exploring` or `accepted` (paired with PRD) anticipated near-term per maintainer-stated priority. | The maintainer assumed distillation triggers were already a load-bearing harness function; their absence is invisible until pointed at, and every session without a trigger leaks institutional knowledge into private agent caches that future agents cannot reach. Filing as OPP now (rather than batching with longer-term R&D) preserves the priority signal and provides the structured surface for the PRD pass that will design the v1 trigger machinery (passive validators vs. active hooks vs. hybrid; heartbeat-formalization or retirement; scope across knowledge-capture/opportunity-capture/agents-claude-code modules). | @unclenate | OPP-0004 |
| 2026-05-20 | Discovery | Documentation pass capturing two architectural observations and two operating-principle additions. (1) Appended to `docs/knowledge/shared-observations.md`: "Companion-rule precision is best enforced by file boundaries, not regex sophistication" (architectural severity; generalizes the ADR-0012 decision) and "Consumer-driven feedback has displaced the scheduled-review cadence as the active quality mechanism" (process severity; flags the missed `distilled-learnings.md` 2026-04-30 review target as governance debt and proposes two paths forward). (2) Added new § 7 to `docs/operating-principles.md`: "Align File Boundaries with Change-Class Boundaries" — first applied in ADR-0012. (3) Added bullet under § 3 (Documentation as Part of the Change): "Module-text reads in stripped contexts" — write `module.yaml` rule text with fully-qualified paths because the validator strips surrounding-file context when emitting human-review prose to CI logs (lesson from PR #29 Copilot review). Synced `shared-observations.md` header `Last Updated` to 2026-05-20 (was 2026-04-16, lagging behind the latest observation by a month). Deliberately did NOT touch `distilled-learnings.md` — the rule on that file requires a `review-log.md` or daily-memory satisfier, neither of which exists in this repo, and silently bumping the missed-review date would be exactly the untraced edit the rule guards against. The honest staleness is now an observation. | The auto-harness flips the usual ratio: private agent memory should be a cache of what's in the harness, not the canonical store. Anything generalizable that an agent would otherwise memorize privately belongs in shared-observations (if observed once) or operating-principles (if it's a durable how-this-project-works truth). User-directed pass following PR #29 merge; consolidated four meta-knowledge surfaces in one commit. | @unclenate | — |
| 2026-05-19 | Governance | `management/opportunity-capture` v1.0.0 → v1.1.0: split candidate index out of `docs/opportunities/README.md` into a sibling `docs/opportunities/candidates.md`. New optional artifact (listed in `module.yaml` `optionalArtifacts`), new template at `platform/templates/opportunity/candidates.md`, dogfooded `docs/opportunities/candidates.md` in the auto-harness repo with the three existing OPPs grouped into two clusters. README companion rule's trigger path is unchanged (still `^docs/opportunities/README\.md$`); its `humanReview` text is sharpened to remind reviewers to move organizational diffs to `candidates.md` before merge. Template README updated to explicitly state the candidate index belongs in `candidates.md`. Module compiled-fragment README adds the new optional artifact and points at the split ADR. | Issue #28: companion rule fires on *any* `README.md` diff — even when the change is purely an index/cluster-heading edit. First hit cleanly in `bdits/municipal-brain` while landing the second OPP batch. The rule's own `humanReview` text already names the distinction it wants to enforce ("not merely a tangential README update") but the validator cannot make that distinction. Splitting the artifact aligns the file boundary with the change-class boundary the rule wants — structural changes stay in the ADR-gated file; organizational changes move to the unguarded file. By construction, no validator-engine change needed. Rejected alternatives: section-aware triggers (over-engineering for one rule), `acceptedAlternative` field for ratification ADRs (treats symptom not cause), inverse split with policy in sibling (worse ergonomics for the binding contract). | @unclenate | ADR-0012 |
| 2026-05-18 | Technical | Fixed stale validator CLI signatures in `platform/skills/harness-governance/SKILL.md` per issue #24. The skill's "Running Validators" code block documented `validate-placeholders.sh harness.manifest.yaml .` — that signature changed in PR #15 to `[<project-root>]` only, so consumers following the skill verbatim get `cd: harness.manifest.yaml: Not a directory` and an exit-2 failure. Also added the two validators the skill omitted (`validate-agent-pack.sh`, `validate-doc-references.sh` — the latter added in PR #22), corrected `validate-companions.sh` invocation to include the third `<base-branch>` positional arg, and added prose notes documenting the per-script signature quirks (placeholders/doc-references take project-root not manifest; companions is PR-diff-based and exits 0 with a "Skipping" message when run locally on a clean branch). Documented the 3-state exit contract (0/1/2) in the same section so consumers know what a non-zero exit means. | Issue #24 caught the drift. PR #15 (uniform --help + 3-state exits) and PR #22 (validate-doc-references v2) changed the validator surface; the skill was a derived doc and didn't get updated in either of those PRs. Without this fix, every consumer following the harness-governance skill verbatim hits the bug on first validator-chain run. Class of bug: validator-surface-vs-skill-doc drift; future-proofing recommendation in the PR body suggests pinning the skill's validator block to a generated/asserted snippet. | @unclenate | — |
| 2026-05-18 | Technical | Audit-closure bundle: L4-08, L4-09, L4-10, L3-03, L1-12, L1-17. (1) Sample-project manifest duplication reframed: the two sample manifests are byte-identical *by design* per ADR-0003's topology-agnostic claim — added explanatory header comment to both files so future readers reach the same conclusion as the README. (2) Discovery-rubric gap: added "Data / ML research pipeline producing reports / artifacts?" row to discovery-to-composition.md decision matrix pointing at `domains/media-pipeline` + `data/object-storage` + `architectures/event-driven` (matches `research-pipeline-python-object-storage.yaml`). (3) Maturity + criticality enum docs expanded in `bootstrap-quickstart.md` and `troubleshooting.md` to enumerate all 5 maturity conventions (prototype/mvp/production/research/platform — schema accepts any string) and all 7 criticality values (low/medium/high/critical/platform/research/internal — schema enforces enum). (4) Defense-in-depth on `base_branch` arg: new `REF_NAME_REGEX` validation in `harness_registry.rb#changed_files` rejects shell-injection attempts (semicolon-curl-pipe-sh patterns), leading-dash flag impersonation (e.g. -rf), backtick subshell expansion, and non-string types before they reach the git backtick call. 6 new unit tests in `TestChangedFilesBaseBranchValidation`. (5) New `legacy/README.md` explains the directory's contents (`v1-monolith-prompt.txt`, `Docs.pdf`, `project-specific/`) and points readers at current-state docs. (6) SPDX header added to `OPP-0001-exportable-governance-contract-for-runtime-harnesses.md` (the audit's lone OPP-without-header). | Audit-closure methodical sweep: each finding is small in isolation but bundling lets one PR description carry the rationale for each + one round of CI rather than 5 micro-PRs. Each finding's classification status (real bug / clarification-needed / cosmetic / defense-in-depth) is documented per-item in the PR body. L4-08 specifically REFRAMED: not the bug the audit alleged; the dup is the central pedagogical point of the submodule-consumer sample per ADR-0003, and the fix is documenting that intent at the manifest level rather than breaking the demonstration. | @unclenate | — |
| 2026-05-18 | Governance | Markdownlint policy + CI gate + `.editorconfig` (Lane 5 closure). New `.markdownlint-cli2.jsonc` tunes the rule set for governance-doc patterns (MD013/MD028/MD036/MD041 disabled with reasons; MD024/033/046/048/049/050/055/056/060 tuned to repo convention). New `markdownlint` CI job runs against 248 in-scope .md files; blocks on errors. New `.editorconfig` codifies LF / no-trailing-whitespace / spaces-over-tabs / 2-space indent (MD-trailing-whitespace exception preserves two-space line-break syntax; Makefile-tab exception preserves POSIX). Repo-wide cleanup: 349 markdownlint findings → 0. `--fix` resolved 280 mechanically; manual: 16 MD040 fenced-code language tags added, 5 MD056 table-column-count bugs fixed (2 sample-project mvp-scope headers, 1 trailing-pipe extra cell, 2 pipe-inside-backtick escapes), 4 MD042 placeholder anchors filled in pointing at SUMMARY.md TOC sections. | Quality-audit Lane 5 had 0 of 8 findings closed; largest unaddressed lane. User explicitly flagged MD060, MD032, MD038 prevalence. Single bundled config + CI gate + sweep PR closes the lane and prevents future drift. Disabled-rule rationale captured in ADR-0011 rather than inline config comments. | @unclenate | ADR-0011 |
| 2026-05-18 | Technical | `validate-doc-references.sh` v2 — renderer-aware scope expansion. New helpers in `platform/validators/lib/harness_registry.rb` (`extract_markdown_links`, `strip_inline_code_spans`, `link_target_external?`, `strip_link_anchor`, `resolve_relative_link`, `link_target_classify`, `link_target_renderer_safe?`, `markdown_files_to_scan`) drive a second validator pass that scans every `*.md` under project root (excluding `legacy/`, `.git/`, `.claude/`, `node_modules/`, `.worktrees/`, `platform/validators/test/fixtures/`, `platform/templates/docs/`) for `[text](target)` links with a relative target. Each target is resolved against the markdown file's directory and classified as `:ok`, `:missing` (broken), `:directory_target` (trailing-slash or resolves-to-directory — GitBook 404s), or `:extensionless` (bare basename like `LICENSE-MIT` — also GitBook-fragile). Inline backtick code spans are now stripped before link extraction so pedagogical link syntax shown inline as code (a backticked `[X](broken.md)`-style link) does not false-positive. The v1 `platform/...` bare-path pass is preserved as a first pass so the existing dogfood guarantee carries over. Six new fixtures (`v2-broken-relative-link/`, `v2-inline-code-link/`, `v2-bare-extensionless/`, `v2-trailing-slash/`, `v2-external-skipped/`, `v2-ignored-by-file/`) plus 6 new integration tests + ~36 new unit tests cover the surface. `.doc-reference-ignore` seeded with the single real v2 finding against the harness's own tree (regex `^docs/adr$` exempting the trailing-slash directory link to `docs/adr/` in CHANGELOG.md:179), tracked in the new "Known v2 exclusions to triage" section of `platform/validators/README.md` for follow-up cleanup. | The renderer-aware bug class (relative-path links, bare extensionless targets, trailing-slash directory targets, inline-code false-positives) cost two PRs in this session (#18 and #20). v1 only matched `platform/...`-shaped strings inside `platform/*.md`, so every cross-cutting doc link and every renderer-fragile pattern slipped through. v2 closes the class statically in CI. Followup-only scope discipline (no link fixes beyond seeding the ignore file) keeps the PR diff small and reviewable; the long-tail cleanup is the next pass. | @unclenate | — |
| 2026-05-18 | Technical | Shellcheck added to CI at `--severity=warning` threshold via new `shellcheck` job in `.github/workflows/harness.yml` (single-OS Linux runner; output identical on macOS so matrix not needed). Direct `apt-get install` invocation chosen over third-party action to avoid supply-chain step. Two `install.sh` cleanups landed alongside: removed dead `dry_prefix()` function (SC2329 — unused); added file-level `# shellcheck disable=SC2034` with reason comment for the `NON_INTERACTIVE` flag (parsed + documented in usage block but currently a no-op since install.sh has no prompts; reserved for future). One info-level finding remains visible in CI logs but non-blocking: SC2012 (`ls` in error-message string at install.sh:145 — contextually fine; replacing with `find -printf` is uglier). Per the audit's Triage discipline, this PR is "add the gate" not "fix everything"; warning-threshold gives the CI gate without the noise of style-level info findings. | Audit finding L2-03: 1,571 lines of bash across 13 `.sh` scripts had no static analysis. PR #12's BSD-awk silent-failure precedent showed exactly the cost — bugs that survived two macOS-test lifetimes because no automation looked. Total findings on the harness's own scripts: 3 (1 warning + 2 info), all in install.sh. Real bugs class clean. | @unclenate | — |
| 2026-05-18 | Technical | Renderer-aware link fixes + audit-win bundle. Entry-point license links (README.md, HARNESS.md, SUMMARY.md, CONTRIBUTING.md) converted from bare `LICENSE-MIT` / `LICENSE-APACHE` / `NOTICE` / `AUTHORS` targets to absolute GitHub URLs (`https://github.com/unclenate/auto-harness/blob/main/...`). Twenty directory-target (trailing-slash) markdown links across 10 profile/template/sample-project READMEs now point at the canonical file inside (`README.md`, `SKILL.md`, or `HARNESS.md`) instead of the directory itself. `platform/validators/README.md` Bash-version contradiction reworded (the 7 `validate-*.sh` scripts work on Bash 3.2+; only bootstrap scripts require Bash 4+). `Regexp.timeout = 1.0` set unconditionally in `platform/validators/lib/harness_registry.rb` to defend against ReDoS from user-supplied `companionRules.{triggerPaths,requiredAny,forbiddenPatterns}` and `.doc-reference-ignore` patterns. New top-level `CHANGELOG.md` (Keep-a-Changelog format) summarizing externally visible changes; cross-references the internal `docs/project/change-log.md` for per-decision audit trail. | The QUALITY-AUDIT-2026-05-18 lane-1 sweep checked filesystem existence of link targets but didn't catch *renderer* breakage: GitBook treats bare extensionless targets as directories and 404s on `<target>/README.md`. User reported a GitBook-published link to `LICENSE-MIT` resolving to a non-existent `LICENSE-MIT/README.md` (returning to `unclenate/auto-harness` via the redirected `unclenate/ai-prompts` old repo name). Same root cause as L1-14 (profile READMEs linking to skill directories). One coordinated PR closes the renderer-aware class for both. Bundled with L1-08 (validators/README Bash version, 1 line), L2-07 (`Regexp.timeout`, 1 line), and L1-09 (CHANGELOG.md, OSS hygiene) since each is a quick win from the audit's open list. | @unclenate | — |
| 2026-05-18 | Governance | Expanded `kernel/base` companion rule's `requiredAny` to accept `^docs/project/change-log\.md$` and `^docs/project/dependency-log\.md$` alongside the existing `ADR-` / `operating-principles.md` / `PRD-` satisfiers. Rewrote the rule's `humanReview` clause to distinguish substantive decisions (require ADR/PRD/operating-principles) from routine maintenance (may satisfy via change-log or dependency-log entry). Updated `docs/operating-principles.md § 2` to document the satisfier-scales-with-change-weight policy. | First Dependabot auto-PR (#17, `actions/checkout v4 → v6`) failed the kernel rule because Dependabot has no way to author ADR/PRD/operating-principles. Heavy-only satisfiers create perpetual friction for all routine dep bumps; bypassing via `--admin` trains the maintainer to ignore the validator. Cheaper satisfiers preserve auditability (rule still fires, every workflow edit still gates) while matching artifact weight to change weight. | @unclenate | ADR-0010 |
| 2026-05-18 | Technical | Catalog count reconciliation across entry-point docs: README.md (skills 5→7 incl. `harness-agentic-interfaces` + `harness-mcp` rows; validators 6→7 incl. `validate-doc-references.sh` row; starter-compositions table gains `interview-driven-discovery.yaml` and `mcp-server-typescript.yaml`; fixes `validate-companions.sh` invocation missing project-root arg; replaces `YOUR-ORG` with `unclenate`; adds macOS Bash 4 prerequisite paragraph). `platform/README.md` maturity claim aligned to Alpha (was "Beta (April 2026)"). `platform/reference/how-to-read.md` catalog counts updated (24/35/6/4 → 26/55/7/7) with invisible comment documenting count recipes; authority-stack diagram bumped (24→26 modules, 6→7 validators, 35→55 templates, 7→13 workflows). `platform/workflow/skills-and-agents.md` 5→7 with two new skill rows. `platform/workflow/submodule-integration.md` "five validators" → "seven validators" and two more `YOUR-ORG` → `unclenate`. Sample-project artifacts (submodule-consumer + node-web-saas-postgres) "six validators" → "seven". `docs/project/dependency-log.md` Ruby ≥ 2.7 → ≥ 3.0 (matches CONTRIBUTING.md and CI pin). | Catalog growth across PRs #5/#6/#9/#10 added two skills (`harness-agentic-interfaces`, `harness-mcp`), one validator (`validate-doc-references.sh`), and two compositions (`interview-driven-discovery.yaml`, `mcp-server-typescript.yaml`), but the entry-point docs never tracked the changes. The QUALITY-AUDIT-2026-05-18 audit (Triage item #1, findings L1-02/03/04/05/06/07/L4-01/04/05/13/02) consolidated 12 stale-count/broken-doc findings as one mechanical fold-back. Bundled as a single PR per audit's recommendation. | @unclenate | — |
| 2026-05-18 | Technical | Added `validate-doc-references.sh` (scans all platform/*.md for `platform/...` refs and asserts each resolves on disk; skips fenced code blocks; honors `.doc-reference-ignore`) and `forbiddenPatterns` companion-rule semantics (hard fail in `validate-companions.sh` when a changed-file path matches a forbidden regex, regardless of `requiredAny` satisfaction; forbidden-check runs first). Converted the codex-cli pack's documentary `AGENTS.override.md` ban into hard enforcement via the new field; bumped codex-cli to 0.2.0 and rewrote the README's "Mitigations" section to drop the "honest-but-documentary" language. CI workflow extended with a `Validate doc references` step. | The original validator chain had no static check for path drift across documentation; doc-reference bugs surfaced only during PR review. The codex-cli `AGENTS.override.md` rule was honest about its limitation (`requiredAny` could be satisfied by any documentary edit alongside the offending file). One coordinated branch closes both gaps by extending the existing companion-rule machinery rather than introducing a new validator script for the forbidden case. | @unclenate | — |
| 2026-05-18 | Scope | Promoted OPP-0002 to `accepted` with Disposition (accepted via ADR-0007, shipped in PR #5 commit 8aef150) and Promotion (ADR-0007 accepted same date, implementation-surface inventory, future PRDs deferred to consumer-driven signal) fields populated. Bumped ADR-0007 to Accepted with today's date. | The agentic-interfaces module set has been shipped and review-validated; the OPP and ADR records need to reflect the now-accepted state instead of remaining in proposed/staged form. | @unclenate | ADR-0007, OPP-0002 |
| 2026-05-18 | Scope | Promoted OPP-0003 to `accepted`, with Disposition and Promotion fields populated to match the now-accepted MCP-awareness direction and implementation state. | ADR-0008 resolved the architecture-vs-domain decision and scoped exposed governance via MCP as read-only/advisory v1; the opportunity record now reflects that accepted outcome instead of remaining in proposed state. | @unclenate | ADR-0008, OPP-0003 |
| 2026-05-17 | Discovery | Filed OPP-0002 (Agentic Interface Awareness) and proposed via ADR-0007. Adds `domains/agentic-interfaces` (cross-cutting module covering design doc, risk register, prompt-tool registry, renderer contract for in-product agent surfaces) and `architectures/agentic-ui` (optional topology overlay for conversational-primary, MCP-host, and Open-ended generative-UI products). Ships template family, `harness-agentic-interfaces` Agent Skill, integration workflow guide, `agentic-ui-saas.yaml` starter composition, and `agentic-ui-starter` sample project. Vendor-neutral at the module level (Controlled / Declarative / Open-ended / Conversational-primary flavor map); CopilotKit, A2UI v0.8, MCP Apps cited as canonical implementations with detail carried in templates and the skill. | The "agentic interface" pattern (CopilotKit-style copilots, A2UI-style generative UI, MCP Apps, conversational-primary products) consolidated across the React/Next.js ecosystem in 2026. Auto-harness had no module that named the shape or governed its risks (prompt-injection via tool results, hallucinated UI affordances, action-approval bypass, generative-UI XSS, renderer-contract drift, model-update regression). Closes the gap on the consumer-side, paralleling OPP-0001 on the runtime-harness side. R&D pass — committed on `feature/agentic-interfaces-rnd` for maintainer review before merge. | @unclenate | OPP-0002, ADR-0007 |
| 2026-05-15 | Scope | Open-source cut: dual MIT/Apache-2.0 licensing, SPDX/copyright headers across all source files via deterministic `platform/bootstrap/add-license-headers.sh` script, new `Maintenance & Operations` workflow section consolidating previously-scattered post-adoption content (upgrade flow, version pinning, drift recovery, lifecycle transitions, periodic audits), TOC reorganization in README.md and SUMMARY.md around an Adoption → Day-to-Day → Maintenance → Reference axis, full community-file foundation (`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` adopting Contributor Covenant v2.1 by reference, `SECURITY.md` with harness-specific threat model, `AUTHORS`, `NOTICE`), GitHub issue forms (bug / feature / observation — the last channeling external contributors into the knowledge-capture flow) and a PR template embedding companion-rule discipline. Attribution standardized to `UncleNate@gmail.com` (personal email for personal IP), distinct from any commercial-project email. | Pre-launch readiness audit surfaced two coupled issues: information architecture conflated adoption, day-to-day, and post-adoption maintenance under a flat "Workflow Guides" heading; and the repository had no legal foundation (no LICENSE, no per-file indicia, no community files). Single coordinated PR addresses both so the public flip is one clean cut. Dual-license pattern (Rust-ecosystem convention) maximizes downstream optionality without imposing NOTICE-only friction. | @unclenate | ADR-0005 |
| 2026-05-12 | Technical | Documentation alignment pass: HARNESS.md Active Modules table, SUMMARY.md (Module Library + Templates sections), README.md directory tree, `platform/skills/harness-onboarding/SKILL.md` management catalog, `platform/templates/README.md` Opportunity section, and `platform/workflow/discovery-to-composition.md` decision rubric all updated to reflect `management/knowledge-capture`, `management/opportunity-capture`, and `agents/openclaw` (the latter two of which had been added without back-propagating to catalog docs). `docs/operating-principles.md` refreshed (Last updated bumped; added explicit catalog-propagation principle to § 3). | Active module catalog had drifted from the canonical docs — `knowledge-capture` and `openclaw` were activated but never propagated; `opportunity-capture` was just added. Section 3 of operating principles explicitly requires catalog updates in the same pass, so this aligns the operating principle with the practice. | @unclenate | — |
| 2026-05-12 | Discovery | Filed OPP-0001 (Exportable Governance Contract for Runtime Harnesses) as the first candidate under the new opportunity-capture module. Paired with a structured observation in `docs/knowledge/shared-observations.md` documenting the runtime-harness vs. governance-harness genre distinction surfaced by reviewing adenhq/hive. Status: proposed; Confidence: medium. | First end-to-end exercise of the opportunity-capture module: the candidate cites the backward-looking observation as its Origin / Evidence, closing the gap between forward-looking ideas and backward-looking observations at the explicit evidence link rather than by merging the two surfaces. | @unclenate | OPP-0001 |
| 2026-05-12 | Scope | Added `management/opportunity-capture` module: new opt-in surface (`docs/opportunities/`) for pre-PRD candidate records with status (proposed / exploring / accepted / declined / superseded), evidence linkage to `docs/knowledge/shared-observations.md`, and a binding promotion contract (status=accepted requires PRD in same commit). Two companion rules wired via existing validators; no new validator code. auto-harness adopts the module in its own manifest. | A session-level analysis surfaced the gap between forward-looking ideas and backward-looking observations: the harness had no native surface for pre-PRD candidates, so product-shaped insights died in conversation history. Closes the gap at the explicit evidence link rather than by merging surfaces. | @unclenate | PRD-0003, ADR-0004 |
| 2026-05-09 | Scope | Extended PRD template with optional execution-spec sections (Goals & Non-Goals, Tech Stack, API & Data, UI/UX Notes, CI/CD Gates) | A talk on hackathon-style PRDs surfaced gaps where the template, optimized as a governance record, doesn't constrain AI-agent build choices. Single template with "when applicable" sections supports both flavors; PRD-0001 backfilled to demonstrate `N/A — governance PRD` convention; companion rules unchanged. | @unclenate | PRD-0002 |
| 2026-04-21 | Technical | Adopted git submodule as first-class consumption model with brownfield-safe bootstrap and multi-platform coexistence | Copy-in adoption caused upstream drift; consumers rarely re-synced; multi-platform repos needed explicit coexistence guarantees. Shipped: `install.sh` + `link-skills.sh` bootstrap tools, `submodule-integration.md` guide, `TestSubmoduleMount` proof, doc updates across `ci-integration.md` / `bootstrap-quickstart.md` / `brownfield-onboarding.md` / `SKILL.md` / `README.md`. | @unclenate | ADR-0003 |
| 2026-04-20 | Technical | Added CI self-governance workflow and installed `harness-governance` skill for self-application | Harness now enforces its own validator chain and self-tests on every PR/push to main, and agents working on the harness operate under the governance skill it provides to projects | @unclenate | — |
| 2026-04-08 | Technical | Bootstrapped `AGENTS.md` as the workspace-instructions entrypoint | Keep one cross-tool source of truth for agent guidance and avoid duplicated Copilot-specific instructions | @unclenate | — |
| 2026-04-07 | Scope | Restored PRDs as first-class record type | PRD process was lost in monolith-to-modular decomposition; product decisions had no structured rationale record | @unclenate | PRD-0001 |
| 2026-04-07 | Scope | Added self-governance artifacts | Harness should eat its own dog food; declared modules but had no artifacts | @unclenate | ADR-0001 |
| 2026-04-07 | Scope | Added product-lite module to manifest | Required for PRD companion rules and product artifact tracking | @unclenate | — |
| 2026-04-07 | Technical | Repo renamed from ai-prompts to auto-harness | Name better reflects the project's purpose as a governance framework | @unclenate | — |
| 2026-04-07 | Technical | Promoted development-harness/ to repo root | Eliminated unnecessary nesting; simplified all internal path references | @unclenate | — |

---

## What Belongs Here

Add an entry when:

- A requirement is added, removed, or significantly changed
- A milestone is moved or dropped
- An architectural decision changes direction
- A feature is explicitly deferred to a future release
- A third-party dependency changes

Do NOT add entries for routine code changes, minor doc fixes, or bug fixes that don't
change scope or direction.

---

## Reference

| Resource | Path |
| -------- | ---- |
| Requirements | `docs/product/requirements.md` |
| ADR directory | `docs/adr/` |
| PRD directory | `docs/requirements/` |
| Milestones | `docs/project/milestones.md` |
