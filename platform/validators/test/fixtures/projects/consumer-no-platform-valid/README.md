# Consumer (No Platform Dir) — Valid Fixture

Simulates a submodule consumer: the harness platform lives under a submodule
(not a top-level `platform/`), so this project root has no `platform/` tree.

Valid relative link to a sibling doc: [setup guide](docs/guide.md).
