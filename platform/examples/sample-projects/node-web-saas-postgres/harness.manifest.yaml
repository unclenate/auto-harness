# NOTE: This is an auto-harness sample-project file (reference implementation).
# If you copy this file into your own project, replace the SPDX/copyright
# header below with your own — running
# `bash platform/bootstrap/set-consumer-headers.sh` from your project root
# after the copy will do this for you.
#
# Copyright 2026 Nate DiNiro <UncleNate@gmail.com>
# SPDX-License-Identifier: MIT OR Apache-2.0
#
# This manifest is also reused byte-identical by
# ../submodule-consumer/harness.manifest.yaml to demonstrate that the
# governance manifest is topology-agnostic per ADR-0003 — the same
# modules activate the same governance regardless of mount topology.
schemaVersion: 1
project:
  id: node-web-saas-postgres
  name: Node Web SaaS Postgres
  maturity: production
  criticality: medium
modules:
  core:
    - kernel/base
  stacks:
    - node-typescript
  architectures:
    - web-app
  data:
    - relational-postgres
  delivery:
    - production-saas
  management:
    - discovery-intake
    - product-lite
    - project-standard
  agents:
    - base
    - claude-code
overrides:
  requiredArtifacts: []
  disabledValidations: []
