# Ignored References Fixture

This bare-path reference is broken but the `.doc-reference-ignore` file exempts it.

See platform/foo/known-missing.md for details.

A real same-dir link also exists: [config](config.yaml).
