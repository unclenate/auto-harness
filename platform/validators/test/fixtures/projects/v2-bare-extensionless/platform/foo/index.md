# v2 Bare Extensionless Fixture

A bare extensionless target that exists on disk but breaks on GitBook:
[license](../../LICENSE-MIT).

GitBook treats `LICENSE-MIT` as a directory and 404s on `LICENSE-MIT/README.md`.
The fix is to use an absolute GitHub URL (or add an extension).
