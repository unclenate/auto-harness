<!--
Copyright 2026 Nate DiNiro <UncleNate@gmail.com>
SPDX-License-Identifier: MIT OR Apache-2.0
Part of auto-harness — see LICENSE-MIT and LICENSE-APACHE at repository root.
-->

# Project Change Log

This log records material changes to project scope, plan, timeline, or technical direction.
It is not a git commit log — it captures *decisions and their rationale*, not code diffs.

---

## Log

| Date | Type | Change | Reason | Owner | ADR/PRD |
| ---- | ---- | ------ | ------ | ----- | ------- |
| 2026-05-23 | Documentation | Wave 2 of audit-driven documentation expansion. Five new authoritative docs filling workflow coverage gaps identified by the 2026-05-23 audit: (1) `platform/workflow/extending-the-harness.md` — module / validator / skill / template / agent-pack author guide for contributors and consumer-organizations extending the harness; explains where each lives, what schemas they follow, what catalog updates they trigger. (2) `platform/workflow/modify-composition-mid-project.md` — operational guide for adding / removing / changing modules in an active manifest, including a sunset section for projects ending harness adoption. Closes the gap where bootstrap covered initial adoption and maintenance covered upstream pulls but nothing covered re-composition. (3) `platform/workflow/incident-response.md` — five-phase operational workflow (stabilize → investigate → document → distill → close) using the existing `incident.md` template; codifies blameless postmortem discipline and the distillation bridge from incidents to operating-principles. (4) `platform/workflow/release-and-versioning.md` — semver policy specific to the harness, pre-1.0 rules, release operational process (CHANGELOG.md structure, git tag procedure, GitHub release flow), deprecation policy, yank semantics, sub-artifact versioning. Sets up Wave 3 work (first version tag) by defining the policy first. (5) `docs/threat-model.md` — companion to SECURITY.md covering asset inventory, five adversary models (curious consumer, malicious contributor, compromised maintainer, adversarial regex input, compromised AI agent), specific attack surfaces (workflow injection, template content, validator scripts, submodule attacks), mitigations in place, and mitigations not yet deployed (with cross-link to OPP candidates). SUMMARY.md TOC reorganized: Day-to-Day Workflows gains cycle-end-distillation + modify-composition + incident-response + multi-agent-tool-coordination rows; Maintenance & Operations gains release-and-versioning; new "Contributing & Extension" section added with extending-the-harness + threat-model. Workflow count bumped 14→18 across all 4 documented assertion sites (caught immediately by `validate-catalog-counts.sh`, fixed in same PR). | Wave 2 of the 2026-05-23 audit trajectory. The audit identified five workflow / capability gaps that were documentation-shaped (vs. machinery-shaped, which is Wave 3). Each new doc closes a specific gap: extending-the-harness for contributor onboarding; modify-composition for the missing operational case between initial adoption and upstream maintenance; incident-response for the gap between SECURITY.md (disclosure) and operational reality; release-and-versioning for the *no-versions-exist* finding; threat-model for the *disclosure-without-threat-modeling* finding. Bundled as one PR per established preference. The catalog-counts validator from PR #41 was the load-bearing safety net: it caught the workflow count drift caused by adding three new docs to `platform/workflow/`, preventing the same class of bug the validator was built to close. | @unclenate | — |
| 2026-05-23 | Documentation | Wave 1 of audit-driven documentation drift closure. (1) `SUMMARY.md` gains nine previously-missing governance artifacts (ADR-0009/0010/0011/0012, PRD-0001/0002/0004/0005, OPP-0004/0005) plus a new "Product Requirements Documents" section between ADR and Opportunity sections; the candidates index also gets a TOC entry. All recent work (PRs #29-#41) is now discoverable from GitBook's table of contents. (2) `README.md` validator count corrected at three call sites (line 97 "seven shell scripts" → "eight"; line 348 "Seven validators" → "Eight"; the validators table gains a new row for `validate-catalog-counts.sh`). (3) `README.md` Reference section gains an entry for `docs/architecture/diagrams.md` with a one-line description naming the six diagrams — closes the gap where the architecture diagrams were reachable only from SUMMARY.md, not from the canonical README entry-point. (4) `validate-catalog-counts.sh` extended with `normalize_count()` for English-number-word forms (one through twenty) plus four new assertions covering README prose word-form claims and the skills-and-agents.md word-form claim. Validator now passes 23 assertions (was 19). One paired observation captured: "Doctrine in prose without enforcement in code is a recurring harness gap pattern" — generalizes the audit's finding across trust-tier enforcement, versioning, knowledge curation, and consumer module operations into a single durable insight. | Quality + capability audit (2026-05-23, this session) identified the drift; this PR closes the documentation drift portion in one bundled commit. Audit pattern: two parallel exploration agents enumerated documentation gaps and missing capabilities; both agents independently surfaced the SUMMARY.md missing-entries issue and the README validator-count drift. Wave 1 is the small/immediate set (~30 min) that unblocks reader discoverability of recent work; Waves 2 and 3 (workflow docs, foundational machinery) follow in subsequent PRs. The observation paired with this PR isn't strictly required by any rule (no trigger paths in diff), but the audit produced a durable architectural insight worth capturing per the workflow doc's recommendation to distill substantive learning even outside trigger boundaries. | @unclenate | — |
| 2026-05-23 | Governance | New `validate-catalog-counts.sh` validator + wired into kernel + CI. Closes the drift class identified in `shared-observations.md` (2026-05-22): *"Each new artifact asserting a catalog count is a new place that fact can drift."* The validator runs inline recipes (`find platform/profiles -name module.yaml \| wc -l`, etc.) to compute canonical counts for seven metrics (modules_profiles, modules_all, validators, skills, templates, workflows, diagrams), then iterates a 19-row assertion table mapping `(file, regex, count-key)` triples to documented claim sites across `platform/reference/how-to-read.md`, `docs/architecture/diagrams.md`, and `docs/_assets/cover-back.svg`. Each documented claim's number is extracted and compared to canonical; drift produces a clear `file: claims N but canonical is M` error and exits 1. Adding the validator bumped `validators` count 7→8, which the validator's own first run caught at four call sites (entry-point prose, ASCII art, diagram-1 label, back cover) — proving the self-stabilization claim by construction. Wired into `kernel/base` module.yaml validators list, into the CI workflow as a new "Validate catalog counts" step between doc-references and companions, and documented in the `harness-governance` SKILL validator chain + signature-notes section. When a new doc cites a catalog count, append a row to the script's `ASSERTIONS` table to keep the drift class closed. | Captured in `shared-observations.md` (PR #39) as a recommendation; OPP-0005's "two classes of token" lesson generalized: text-representation artifacts asserting the same fact must be kept in sync or they decay. Pure structural fix — no human discipline required at PR time once landed. Self-stabilization confirmed (this PR's own validator-count bump exercised the validator on its first run, which caught the doc drift the bump itself created — same dynamic as PR #34's hook adapter catching the rule's regex-scope bug). | @unclenate | — |
| 2026-05-23 | Governance | Promoted "Prefer Text Representations" to `docs/operating-principles.md` as new § 8. The principle codifies an existing pattern in the harness — YAML modules, Bash validators, Markdown SKILL files, Mermaid diagrams, SVG covers, etc. all chose text over binary on independent design pressure; § 8 names the pattern as durable doctrine. Section enumerates the five reasons (versionable, toolchain-free, CI-friendly, diffable, future-proof), an applied-vs-rejected table covering eight surfaces, three legitimate exception classes (photography/illustration; downstream render targets; round-trip-required binaries), and the test question for new artifacts: *"can this be expressed as text without losing essential meaning?"* Header `Last updated` bumped to reflect the § 8 addition. | The pattern accumulated enough instances (YAML manifests + Bash validators + Markdown docs + Mermaid diagrams + SVG covers) across enough independent design decisions that it qualifies as a durable how-this-project-works truth per the operating-principles bar ("true regardless of who is doing the work, what they are working on, and when"). Naming it explicitly turns an implicit convention into a citable design constraint — future contributors and consumers can point at § 8 when arguing against a binary artifact addition. Cheap-satisfier discipline (ADR-0010) applies: no new ADR/PRD spawned because this is synthesis-of-existing-pattern rather than a new design decision; change-log entry is the appropriate weight. | @unclenate | — |
| 2026-05-22 | Documentation | Documentation visualization + QA pass. (1) New `docs/architecture/diagrams.md` central hub with six Mermaid diagrams covering composition, trust-tier decision flow, companion-rule firing (trigger → satisfier → CI gate), opportunity-to-decision lifecycle, distillation-trigger composition (active hook + passive rule), and consumer adoption end-to-end. Each diagram has prose framing, color-coded states (pass/fail/blocked/escalation), and a "how it composes with the others" section at the bottom. (2) Six targeted embeds: HARNESS.md (overview pointer), `harness-governance` SKILL.md (trust-tier + companion-rule callouts), `cycle-end-distillation.md` (distillation diagram link), `opportunity-capture/README.md` (lifecycle diagram link), `submodule-integration.md` (consumer-adoption diagram link), SUMMARY.md (TOC entry in Concepts and Reference section). (3) QA pass fixing stale catalog counts in `platform/reference/how-to-read.md`: `55 templates → 56` in prose line; `(55 files) → (56 files)` and `(13 guides) → (14 guides)` in the authority-stack ASCII art. Module count `26` confirmed correct per the documented recipe (profile-only count, excluding core/agents). (4) Two cover SVGs at `docs/_assets/cover-front.svg` and `cover-back.svg` for GitBook PDF / print export — 1600×2400 (2:3) bold-typography + nested-rectangle composition motif; cream background, deep-slate accent; cleanly rescales to US Letter / A4 / trade. Self-documenting `docs/_assets/README.md` covers the palette, style intent, GitBook integration notes, and the SVG-over-PNG rationale. (5) One paired architectural observation: "Each new artifact asserting a catalog count is a new place that fact can drift" — captures the maintenance cost added by the new diagrams + back cover both restating the same catalog counts; recommends an eventual `validate-catalog-counts.sh` validator while documenting the human-discipline workaround in the meantime. | Visual + cross-reference communication asked for to support team-member onboarding, repo-user comprehension, and GitBook PDF/print exports. Six diagrams chosen to cover the system's six conceptually distinct slices (composition / authority / enforcement / lifecycle / distillation / adoption) — fewer would have left gaps, more would have produced redundancy. Mermaid chosen over external SVG/PNG because it's text-based (versionable, diffable, no toolchain), GitBook renders it natively, and editing happens in the same file as the prose context. SVG covers chosen over raster (PNG/JPG) for the same versionability + scale-without-pixellation reasons; rsvg-convert instructions documented for downstream tools that need raster. QA pass deliberately minimal — only true stale-count claims fixed, no retroactive edits to historical ADRs/PRDs/release-intent records (those are temporal snapshots, not auto-updating catalogs). | @unclenate | — |
| 2026-05-22 | Governance | PRD-0005 v1 fully shipped + OPP-0005 promoted `exploring` → `accepted`. Five FRs landed: (FR-001) 61 template files under `platform/templates/**` had their literal SPDX/copyright headers replaced with token forms (`YEAR`, `OWNER_NAME`, `OWNER_EMAIL`, `SPDX_LICENSE` in `[[…]]` brackets); the `Part of auto-harness` provenance line dropped (consumer copies shouldn't claim derivation). All 61 templates had byte-identical headers pre-change so the transformation was a single mechanical pass. (FR-002) New `platform/bootstrap/set-consumer-headers.sh` (250 lines, pure bash, shellcheck-clean) — interactive prompts by default with TTY auto-detection; flag-driven for CI / scripted use; reads / writes `.harness-headers.yaml` project-local config so subsequent template scaffolds auto-fill; supports `--dry-run`, `--files=path1,...`, `--scan=DIR`; portable BSD/GNU sed in-place edit with backup-suffix pattern; basic email-shape validation. (FR-003) 78 sample-project files (70 markdown, 5 yaml, 3 shell) gained a leading re-attribution marker comment block (per-extension comment style — HTML for md, `#` for sh/yaml) explaining that derivative copies must re-attribute via the bootstrap helper. Existing SPDX block preserved (sample-projects are worked examples authored by the maintainer). (FR-004) Two-occurrence attribution drift in `platform/bootstrap/add-license-headers.sh` fixed (line 2 header + line 64 `AUTHOR=` variable — one more than the OPP flagged; the audit's original recommendation 2026-05-18 to add a CI guard remains future work). (FR-005) `platform/bootstrap/README.md` gained "Setting consumer project headers" section + new tool-table row + cross-link to the helper; `platform/templates/README.md` gained the "two classes of token" callout documenting the project-wide-vs-per-record distinction the bootstrap helper enforces. Architectural observation captured: "Header-token classes split cleanly along project-wide vs. per-record axis" — generalizes the design pressure from PRD-0005 v1 to future tokenized surfaces. PRD-0005 status Proposed → Accepted; OPP-0005 status exploring → accepted; candidates.md row reflects full v1 shipped. | OPP-0005 acceptance criteria 1-4 all met — PRD Accepted, FRs all merged, auto-harness self-dogfoods (the 61 tokenized templates pass validation via existing `platform/**` exemption in `.placeholder-ignore`), end-to-end smoke test verified (template → cp → `set-consumer-headers.sh` → filled output). v1 deliberately scoped to forward-fix templates and ship the bootstrap helper; consumer-migration tool for already-inherited bad headers deferred to follow-up per PRD § Future Work, with manual remediation pattern documented in the v1 PR. PRD-design pressure surfaced the project-wide-vs-per-record token-class distinction that wasn't visible at OPP time — captured as durable architectural learning. Closes OPP-0005 v1. | @unclenate | PRD-0005, OPP-0005 |
| 2026-05-22 | Scope | Promoted `OPP-0005 — Consumer Header Hygiene` from `proposed` to `exploring` (same-day flip after filing) with Disposition populated: direction set on **Option A + Option B** from the four candidates — tokenize template headers so the existing `validate-placeholders.sh` machinery gates new files, plus a small consumer-facing bootstrap helper to fill tokens project-wide. Option C (full `management/header-hygiene` module) deferred as premature primitive-creation; Option D (strip headers entirely) declined because templates lose pedagogical value. Sample-projects retain attribution but gain re-attribution markers. Drafted `PRD-0005 — Consumer Header Hygiene — Stop Template Attribution Leakage` as the paired design. PRD specifies v1 as five coordinated pieces: (FR-001) tokenize every header block under `platform/templates/**` with uppercase token forms that match the existing placeholder validator's regex; (FR-002) ship `platform/bootstrap/set-consumer-headers.sh` — interactive + flag-driven helper that writes a project-local config (`.harness-headers.yaml`) and substitutes tokens across template-derived files; (FR-003) add re-attribution leading-comment markers to every sample-project file (sample-projects are worked examples, not scaffolding sources); (FR-004) fix one-line internal drift in `platform/bootstrap/add-license-headers.sh` line 2 (work email → personal email per the project's own attribution rule); (FR-005) document the consumer onboarding flow in `platform/bootstrap/README.md` and add a tokenization note to `platform/templates/README.md`. PRD takes positions on all six OPP-0005 open questions, including: tokenize SPDX (not just copyright + email) because consumer licensing is genuinely their choice; config-file over per-prompt because lower friction and regeneratable; script in `platform/bootstrap/` rather than a new module because header hygiene is a consumer-onboarding concern not a governance-policy concern. Explicit non-goals: consumer-migration tool for already-inherited bad headers (follow-up); `validate-headers.sh` post-hoc detector (token-validator gate at template-creation is the v1 floor); language coverage beyond the existing extension list. Acceptance criteria for OPP-0005 → `accepted`: PRD-0005 Accepted + FR-001..FR-005 merged + auto-harness self-dogfoods + one verified consumer / fixture bootstrap. Bumped OPP-0005 candidates.md entry `(proposed)` → `(exploring; PRD-0005 in flight, drafted 2026-05-22)`. One process observation captured in `shared-observations.md`: "PRD drafts surface questions the originating OPP successfully elided — the OPP→PRD pipeline is a discipline, not a redundancy" — generalizing the pattern that document pairs apply different kinds of design pressure (sibling to PRD-0004's paired-mechanism finding, lifted from validator pairs to document pairs). | Same-day promotion preserves the maintainer-priority signal (per OPP-0004's "Maintainer 'I thought that was already happening' is the highest-signal gap-discovery pattern") and provides the structured surface to drive the implementation PR. PRD scope-disciplined to ride existing machinery wherever possible (cheap-satisfier discipline from ADR-0010 + file-boundary-as-precision from ADR-0012 both informing the design); deliberately resists module-creation and validator-creation in favor of composing primitives already in-tree. PRD design pressure surfaced three OPP-elided decisions (tokenize SPDX; config-driven; no new module) that only had defensible answers once the implementation surface was specified — observation captures that dynamic as durable process learning. | @unclenate | PRD-0005, OPP-0005 |
| 2026-05-22 | Discovery | Filed `OPP-0005 — Consumer Header Hygiene` capturing a legal-correctness gap surfaced by the maintainer in conversation post-PR-#34: 61 template files in `platform/templates/**` and every sample-project file ship with literal auto-harness SPDX/copyright headers (the maintainer's name and email, dual MIT/Apache-2.0), so consumers who scaffold ADRs/PRDs/observations from templates inherit the wrong attribution and license. Asymmetric tooling confirmed: `platform/bootstrap/add-license-headers.sh` operates only on auto-harness's own tree (no consumer equivalent); `validate-placeholders.sh` already exists and would gate `[[…]]`-style fills if templates were tokenized (machinery present but not composed with templates). OPP enumerates four design options (A: tokenize templates only; B: tokenize + bootstrap helper; C: full `management/header-hygiene` module; D: strip headers from templates entirely) with explicit tradeoffs. Initial bias: A+B (cheapest viable v1 leveraging existing placeholder validator, plus a small consumer-facing bootstrap helper). Side-finding: `add-license-headers.sh` line 2 attributes itself to the wrong email (work, not personal) — internal drift to fix in the same implementation pass. Added new `candidates.md` cluster "Consumer onboarding & project hygiene" + OPP-0005 row; bumped OPP-0004 row to reflect full v1 shipped. Status: proposed; Confidence: high. One architectural observation captured in `shared-observations.md`: "Harness primitives that don't compose toward the consumer-side surface are silent governance gaps" — generalizing the diagnosis pattern (primitive A + primitive B both exist; consumer-side surface C needs A∘B; nothing composes them) for future audit passes. | Maintainer-noticed legal correctness issue (per OPP-0004's "Maintainer 'I thought that was already happening' is the highest-signal gap-discovery pattern"). Filing as OPP now (rather than implementing directly) preserves the priority signal, captures the four candidate designs with tradeoffs for PRD-pass alignment, and surfaces the meta-pattern (asymmetric composition of existing primitives) as durable architectural learning. This is the first OPP filed *after* the distillation-trigger rule went live; the rule fires (OPP path matches) and is satisfied (paired observation written) — demonstrating the PR #33+#34 machinery working on its first real cycle outside its own introducing PRs. | @unclenate | OPP-0005 |
| 2026-05-22 | Governance | `agents/claude-code` v1.0.0 → v1.1.0: shipped PRD-0004 FR-006/FR-007 — the Claude Code `Stop`-event hook adapter that emits a structured cycle-end distillation prompt in-session when the current branch carries distillation-worthy work (new ADR / OPP / module manifest / catalog change) and no knowledge destination has been touched. (1) New reference implementation at `platform/examples/sample-projects/node-web-saas-postgres/.claude/hooks/distillation-prompt.sh` — silent-exit fast-paths for non-git, detached-HEAD, on-base-branch, no-base-ref, no-diff, no-trigger, and already-satisfied cases; logs each firing to `.claude/logs/distillation.log` for audit traceability; honors `HARNESS_BASE_BRANCH` env override for non-`main` projects. (2) `agents/claude-code/module.yaml` lists the script under `optionalArtifacts`; `README.md` carries an install snippet for `.claude/settings.json` Stop-hook registration. (3) `.gitignore` gains a scoped negation (`!platform/examples/sample-projects/*/.claude/`) so checked-in reference implementations under sample-projects ship alongside the consumer-runtime `.claude/` ignore — the boundary the ignore was doing precision-work for is preserved, but tracked reference code no longer collides with it. (4) Workflow doc `platform/workflow/cycle-end-distillation.md` § How Agents Should Use This cross-links the hook at step 2 ("in-session reminder; rule is the floor"); table row updated. (5) PRD-0004 status `Accepted` (v1 functional requirements all shipped); OPP-0004 Disposition extended noting follow-up landed. (6) Bug fix during implementation: companion rule's trigger regex broadened from `^platform/profiles/.+/module\.yaml$` to `^platform/.+/module\.yaml$` so the rule covers the 8 agent-pack modules under `platform/agents/*` and the kernel module under `platform/core/kernel/base/`, not just profile modules. Applied in four places (rule's triggerPaths, hook script's grep regex + comment block, workflow doc table, skill table). (7) One fresh architectural observation captured in `shared-observations.md` — "Paired-mechanism implementation is a free correctness check on the governance side of the pair" — codifying the bug-discovery dynamic (writing the second mirror of a regex pattern forces re-derivation that catches silent-narrowness in the first). | The follow-up PR PRD-0004 anticipated. The hook adapter is the *active* layer; the companion rule shipped in PR #33 is the *passive* PR-boundary floor. Both fire on the same change classes; the hook prompts in-session before the rule trips in CI. Side benefit not anticipated: the implementation pass surfaced and fixed a real scope bug in the rule itself, because writing the hook's trigger-detection logic forced enumeration of every place a module manifest lives under `platform/`. Paired mechanisms are a free correctness check on each other — a sibling discovery dynamic to property-based testing applied to governance regex. Closes OPP-0004 follow-up scope; the v1 trigger machinery (validator + hook) is now fully shipped. | @unclenate | PRD-0004, OPP-0004 |
| 2026-05-22 | Governance | `management/knowledge-capture` v1.0.0 → v1.1.0: shipped the v1 cycle-end distillation trigger machinery per PRD-0004. (1) Added a fourth companion rule to `platform/profiles/management/knowledge-capture/module.yaml` — triggers on new/modified ADR (`docs/adr/ADR-*`), OPP (`docs/opportunities/OPP-*`), module manifest (`platform/profiles/*/module.yaml`), or active-module catalog (`harness.manifest.yaml`); satisfiers are any of the three knowledge destinations (`shared-observations.md`, `operating-principles.md`, `distilled-learnings.md`); `humanReview` text demands substantive connection between trigger work and distillation entry (no cargo-cult observations). Rides existing `validate-companions.sh` machinery — no new validator code. (2) New canonical workflow doc `platform/workflow/cycle-end-distillation.md` defines cycle-end signals, the satisfier decision tree, composition with existing audit-trail rules, anti-patterns (cargo-cult observations, distillation-fatigue avoidance, pre-emptive bypass via overrides, auto-generated entries), and how agents + humans should use the pattern. (3) Heartbeat prose in `knowledge-capture/README.md` § Agent Behavior step 5 and `opportunity-capture/README.md` § Agent Behavior step 5 cross-links the new workflow doc, replacing aspirational documentation-only references with actionable contract. (4) `harness-governance` skill SKILL.md gains a new row in the common-companion-rules table + a callout block referencing the workflow doc. (5) `docs/operating-principles.md` § 3 gains a new bullet codifying the trigger-side-vs-destination-side rule duality (Last updated bumped 2026-05-20 → 2026-05-22). (6) OPP-0004 status `exploring` → `accepted`; PRD-0004 status `Proposed` → `Accepted`; candidates.md entry annotated to reflect acceptance. (7) Implementation pass produced one new architectural observation in `shared-observations.md` — "Distillation triggers can land without bootstrap exception when the introducing PR carries genuine learning" — captured naturally as the PR satisfied its own new rule by construction without using `overrides.disabledValidations`. | PRD-0004's v1 minimum-viable trigger machinery shipped in a single coherent PR following the cadence: PRD draft → review → implementation. Achieves OPP-0004 acceptance criteria 1-4 (PRD Accepted; rule landed; dogfooded firing; OPP flipped to accepted). The active-hook adapter (FR-006/007) is should-have follow-up work tracked under the same PRD, may land in a second PR. PR satisfies its own new rule by construction — observed and captured as a fresh meta-observation about valuable rule introductions being self-stabilizing. | @unclenate | PRD-0004, OPP-0004 |
| 2026-05-22 | Discovery | Manual distillation pass on PR #32 (the PRD-0004 draft branch) capturing two architectural/process observations *as proof that the maintainer can do manually what the system PRD-0004 specifies will soon automate*. (1) "Companion-rule machinery was already most of the gap-closer for distillation" (architectural; verified by mapping PRD-0004's pseudocode against existing `validate-companions.sh` fields with no engine change required — the harness's existing primitives reach further than current docs claim). (2) "Maintainer 'I thought that was already happening' is the highest-signal gap-discovery pattern" (process; codifies what made OPP-0004 itself a P0 — assumed-features-as-gaps are qualitatively different from audit-discovered drift). Synced shared-observations.md `Last Updated` to 2026-05-22. | The user explicitly requested a distillation pass before merging PR #32, framing it as "run a distillation and then merge — commence the suggested cadence." Beautiful inversion: distilling BEFORE the trigger machinery lands proves the cycle the trigger will soon automate. Both observations are durable architectural truths derived from this PRD-design cycle, not session-specific artifacts. The first observation argues for "audit existing primitives before building new machinery" as a recurring discipline; the second proposes "assumed-feature-as-gap mining" as a gap-discovery technique. | @unclenate | OPP-0004, PRD-0004 |
| 2026-05-21 | Scope | Promoted `OPP-0004 — Distillation Triggers` from `proposed` to `exploring` (same-day flip after filing); Disposition populated with current investigation direction (hybrid passive + active trigger layer scoped as additions to `management/knowledge-capture` + `agents/claude-code`, heartbeat prose to be formalized into workflow rather than retired). Drafted `PRD-0004 — Distillation Triggers — Cycle-End Trigger Machinery for Institutional Knowledge` as the paired design. PRD specifies v1 as three coordinated pieces: (1) new companion rule on `management/knowledge-capture/module.yaml` declaring distillation-worthy signals (new ADR, new OPP, OPP `proposed` flip, new module manifest, harness.manifest.yaml change) as triggers and the three knowledge destinations as satisfiers — rides existing `validate-companions.sh` machinery, no new validator code; (2) Claude Code `Stop`/`SessionEnd` hook adapter under `platform/agents/claude-code/` emitting a structured distillation prompt from session commit pattern; (3) new workflow doc `platform/workflow/cycle-end-distillation.md` codifying cycle-end signals + satisfier decision tree + composition with existing rules + anti-patterns. Explicit non-goals: Cursor/Copilot/Codex adapters, commit-message scanning, auto-generated entries, threshold-batched distillation, new `management/distillation-cadence` module, retirement of the scheduled-review cadence. OPP-0004 candidates.md entry annotated `(exploring; PRD-0004 in flight)`. | Maintainer-stated core-function gap flagged immediately post-PR #30 merge. Same-day OPP filing + promotion + PRD draft preserves the priority signal and provides the structured surface to drive the implementation PR. PRD takes positions on all six open questions from OPP-0004 with explicit "v1 will validate" criteria, scope-disciplined to ride existing machinery wherever possible (ADR-0010 cheap-satisfier discipline + ADR-0012 file-boundary-as-precision both informing the design). Acceptance criteria for OPP-0004 → `accepted` defined: PRD lands as `Accepted` + companion rule lands in knowledge-capture + auto-harness dogfoods with one real distillation-trail entry. | @unclenate | PRD-0004, OPP-0004 |
| 2026-05-21 | Discovery | Filed `OPP-0004 — Distillation Triggers (Closing the Cycle-End Gap)` capturing a core-function gap surfaced by the maintainer immediately following PR #30: auto-harness provides the *destinations* for distilled knowledge (shared-observations, distilled-learnings, operating-principles, ADRs) but provides no *triggers* that reliably cause distillation to happen during or after a work cycle. The harness's "heartbeat with Knowledge Contribution step" prose is aspirational documentation, not actionable machinery — no skill, validator, or hook enforces it. The recently-merged observation about scheduled-review cadence going untriggered (PR #30) addressed *what's replacing* the cadence (consumer-driven issues); this OPP addresses *why the cadence didn't fire in the first place* and what trigger layer would. Status: proposed; Confidence: high. Added to `docs/opportunities/candidates.md` under new cluster "Knowledge distillation & self-improvement loop." Promotion to `exploring` or `accepted` (paired with PRD) anticipated near-term per maintainer-stated priority. | The maintainer assumed distillation triggers were already a load-bearing harness function; their absence is invisible until pointed at, and every session without a trigger leaks institutional knowledge into private agent caches that future agents cannot reach. Filing as OPP now (rather than batching with longer-term R&D) preserves the priority signal and provides the structured surface for the PRD pass that will design the v1 trigger machinery (passive validators vs. active hooks vs. hybrid; heartbeat-formalization or retirement; scope across knowledge-capture/opportunity-capture/agents-claude-code modules). | @unclenate | OPP-0004 |
| 2026-05-20 | Discovery | Documentation pass capturing two architectural observations and two operating-principle additions. (1) Appended to `docs/knowledge/shared-observations.md`: "Companion-rule precision is best enforced by file boundaries, not regex sophistication" (architectural severity; generalizes the ADR-0012 decision) and "Consumer-driven feedback has displaced the scheduled-review cadence as the active quality mechanism" (process severity; flags the missed `distilled-learnings.md` 2026-04-30 review target as governance debt and proposes two paths forward). (2) Added new § 7 to `docs/operating-principles.md`: "Align File Boundaries with Change-Class Boundaries" — first applied in ADR-0012. (3) Added bullet under § 3 (Documentation as Part of the Change): "Module-text reads in stripped contexts" — write `module.yaml` rule text with fully-qualified paths because the validator strips surrounding-file context when emitting human-review prose to CI logs (lesson from PR #29 Copilot review). Synced `shared-observations.md` header `Last Updated` to 2026-05-20 (was 2026-04-16, lagging behind the latest observation by a month). Deliberately did NOT touch `distilled-learnings.md` — the rule on that file requires a `review-log.md` or daily-memory satisfier, neither of which exists in this repo, and silently bumping the missed-review date would be exactly the untraced edit the rule guards against. The honest staleness is now an observation. | The auto-harness flips the usual ratio: private agent memory should be a cache of what's in the harness, not the canonical store. Anything generalizable that an agent would otherwise memorize privately belongs in shared-observations (if observed once) or operating-principles (if it's a durable how-this-project-works truth). User-directed pass following PR #29 merge; consolidated four meta-knowledge surfaces in one commit. | @unclenate | — |
| 2026-05-19 | Governance | `management/opportunity-capture` v1.0.0 → v1.1.0: split candidate index out of `docs/opportunities/README.md` into a sibling `docs/opportunities/candidates.md`. New optional artifact (listed in `module.yaml` `optionalArtifacts`), new template at `platform/templates/opportunity/candidates.md`, dogfooded `docs/opportunities/candidates.md` in the auto-harness repo with the three existing OPPs grouped into two clusters. README companion rule's trigger path is unchanged (still `^docs/opportunities/README\.md$`); its `humanReview` text is sharpened to remind reviewers to move organizational diffs to `candidates.md` before merge. Template README updated to explicitly state the candidate index belongs in `candidates.md`. Module compiled-fragment README adds the new optional artifact and points at the split ADR. | Issue #28: companion rule fires on *any* `README.md` diff — even when the change is purely an index/cluster-heading edit. First hit cleanly in `bdits/municipal-brain` while landing the second OPP batch. The rule's own `humanReview` text already names the distinction it wants to enforce ("not merely a tangential README update") but the validator cannot make that distinction. Splitting the artifact aligns the file boundary with the change-class boundary the rule wants — structural changes stay in the ADR-gated file; organizational changes move to the unguarded file. By construction, no validator-engine change needed. Rejected alternatives: section-aware triggers (over-engineering for one rule), `acceptedAlternative` field for ratification ADRs (treats symptom not cause), inverse split with policy in sibling (worse ergonomics for the binding contract). | @unclenate | ADR-0012 |
| 2026-05-18 | Technical | Fixed stale validator CLI signatures in `platform/skills/harness-governance/SKILL.md` per issue #24. The skill's "Running Validators" code block documented `validate-placeholders.sh harness.manifest.yaml .` — that signature changed in PR #15 to `[<project-root>]` only, so consumers following the skill verbatim get `cd: harness.manifest.yaml: Not a directory` and an exit-2 failure. Also added the two validators the skill omitted (`validate-agent-pack.sh`, `validate-doc-references.sh` — the latter added in PR #22), corrected `validate-companions.sh` invocation to include the third `<base-branch>` positional arg, and added prose notes documenting the per-script signature quirks (placeholders/doc-references take project-root not manifest; companions is PR-diff-based and exits 0 with a "Skipping" message when run locally on a clean branch). Documented the 3-state exit contract (0/1/2) in the same section so consumers know what a non-zero exit means. | Issue #24 caught the drift. PR #15 (uniform --help + 3-state exits) and PR #22 (validate-doc-references v2) changed the validator surface; the skill was a derived doc and didn't get updated in either of those PRs. Without this fix, every consumer following the harness-governance skill verbatim hits the bug on first validator-chain run. Class of bug: validator-surface-vs-skill-doc drift; future-proofing recommendation in the PR body suggests pinning the skill's validator block to a generated/asserted snippet. | @unclenate | — |
| 2026-05-18 | Technical | Audit-closure bundle: L4-08, L4-09, L4-10, L3-03, L1-12, L1-17. (1) Sample-project manifest duplication reframed: the two sample manifests are byte-identical *by design* per ADR-0003's topology-agnostic claim — added explanatory header comment to both files so future readers reach the same conclusion as the README. (2) Discovery-rubric gap: added "Data / ML research pipeline producing reports / artifacts?" row to discovery-to-composition.md decision matrix pointing at `domains/media-pipeline` + `data/object-storage` + `architectures/event-driven` (matches `research-pipeline-python-object-storage.yaml`). (3) Maturity + criticality enum docs expanded in `bootstrap-quickstart.md` and `troubleshooting.md` to enumerate all 5 maturity conventions (prototype/mvp/production/research/platform — schema accepts any string) and all 7 criticality values (low/medium/high/critical/platform/research/internal — schema enforces enum). (4) Defense-in-depth on `base_branch` arg: new `REF_NAME_REGEX` validation in `harness_registry.rb#changed_files` rejects shell-injection attempts (semicolon-curl-pipe-sh patterns), leading-dash flag impersonation (e.g. -rf), backtick subshell expansion, and non-string types before they reach the git backtick call. 6 new unit tests in `TestChangedFilesBaseBranchValidation`. (5) New `legacy/README.md` explains the directory's contents (`v1-monolith-prompt.txt`, `Docs.pdf`, `project-specific/`) and points readers at current-state docs. (6) SPDX header added to `OPP-0001-exportable-governance-contract-for-runtime-harnesses.md` (the audit's lone OPP-without-header). | Audit-closure methodical sweep: each finding is small in isolation but bundling lets one PR description carry the rationale for each + one round of CI rather than 5 micro-PRs. Each finding's classification status (real bug / clarification-needed / cosmetic / defense-in-depth) is documented per-item in the PR body. L4-08 specifically REFRAMED: not the bug the audit alleged; the dup is the central pedagogical point of the submodule-consumer sample per ADR-0003, and the fix is documenting that intent at the manifest level rather than breaking the demonstration. | @unclenate | — |
| 2026-05-18 | Governance | Markdownlint policy + CI gate + `.editorconfig` (Lane 5 closure). New `.markdownlint-cli2.jsonc` tunes the rule set for governance-doc patterns (MD013/MD028/MD036/MD041 disabled with reasons; MD024/033/046/048/049/050/055/056/060 tuned to repo convention). New `markdownlint` CI job runs against 248 in-scope .md files; blocks on errors. New `.editorconfig` codifies LF / no-trailing-whitespace / spaces-over-tabs / 2-space indent (MD-trailing-whitespace exception preserves two-space line-break syntax; Makefile-tab exception preserves POSIX). Repo-wide cleanup: 349 markdownlint findings → 0. `--fix` resolved 280 mechanically; manual: 16 MD040 fenced-code language tags added, 5 MD056 table-column-count bugs fixed (2 sample-project mvp-scope headers, 1 trailing-pipe extra cell, 2 pipe-inside-backtick escapes), 4 MD042 placeholder anchors filled in pointing at SUMMARY.md TOC sections. | Quality-audit Lane 5 had 0 of 8 findings closed; largest unaddressed lane. User explicitly flagged MD060, MD032, MD038 prevalence. Single bundled config + CI gate + sweep PR closes the lane and prevents future drift. Disabled-rule rationale captured in ADR-0011 rather than inline config comments. | @unclenate | ADR-0011 |
| 2026-05-18 | Technical | `validate-doc-references.sh` v2 — renderer-aware scope expansion. New helpers in `platform/validators/lib/harness_registry.rb` (`extract_markdown_links`, `strip_inline_code_spans`, `link_target_external?`, `strip_link_anchor`, `resolve_relative_link`, `link_target_classify`, `link_target_renderer_safe?`, `markdown_files_to_scan`) drive a second validator pass that scans every `*.md` under project root (excluding `legacy/`, `.git/`, `.claude/`, `node_modules/`, `.worktrees/`, `platform/validators/test/fixtures/`, `platform/templates/docs/`) for `[text](target)` links with a relative target. Each target is resolved against the markdown file's directory and classified as `:ok`, `:missing` (broken), `:directory_target` (trailing-slash or resolves-to-directory — GitBook 404s), or `:extensionless` (bare basename like `LICENSE-MIT` — also GitBook-fragile). Inline backtick code spans are now stripped before link extraction so pedagogical link syntax shown inline as code (a backticked `[X](broken.md)`-style link) does not false-positive. The v1 `platform/...` bare-path pass is preserved as a first pass so the existing dogfood guarantee carries over. Six new fixtures (`v2-broken-relative-link/`, `v2-inline-code-link/`, `v2-bare-extensionless/`, `v2-trailing-slash/`, `v2-external-skipped/`, `v2-ignored-by-file/`) plus 6 new integration tests + ~36 new unit tests cover the surface. `.doc-reference-ignore` seeded with the single real v2 finding against the harness's own tree (regex `^docs/adr$` exempting the trailing-slash directory link to `docs/adr/` in CHANGELOG.md:179), tracked in the new "Known v2 exclusions to triage" section of `platform/validators/README.md` for follow-up cleanup. | The renderer-aware bug class (relative-path links, bare extensionless targets, trailing-slash directory targets, inline-code false-positives) cost two PRs in this session (#18 and #20). v1 only matched `platform/...`-shaped strings inside `platform/*.md`, so every cross-cutting doc link and every renderer-fragile pattern slipped through. v2 closes the class statically in CI. Followup-only scope discipline (no link fixes beyond seeding the ignore file) keeps the PR diff small and reviewable; the long-tail cleanup is the next pass. | @unclenate | — |
| 2026-05-18 | Technical | Shellcheck added to CI at `--severity=warning` threshold via new `shellcheck` job in `.github/workflows/harness.yml` (single-OS Linux runner; output identical on macOS so matrix not needed). Direct `apt-get install` invocation chosen over third-party action to avoid supply-chain step. Two `install.sh` cleanups landed alongside: removed dead `dry_prefix()` function (SC2329 — unused); added file-level `# shellcheck disable=SC2034` with reason comment for the `NON_INTERACTIVE` flag (parsed + documented in usage block but currently a no-op since install.sh has no prompts; reserved for future). One info-level finding remains visible in CI logs but non-blocking: SC2012 (`ls` in error-message string at install.sh:145 — contextually fine; replacing with `find -printf` is uglier). Per the audit's Triage discipline, this PR is "add the gate" not "fix everything"; warning-threshold gives the CI gate without the noise of style-level info findings. | Audit finding L2-03: 1,571 lines of bash across 13 `.sh` scripts had no static analysis. PR #12's BSD-awk silent-failure precedent showed exactly the cost — bugs that survived two macOS-test lifetimes because no automation looked. Total findings on the harness's own scripts: 3 (1 warning + 2 info), all in install.sh. Real bugs class clean. | @unclenate | — |
| 2026-05-18 | Technical | Renderer-aware link fixes + audit-win bundle. Entry-point license links (README.md, HARNESS.md, SUMMARY.md, CONTRIBUTING.md) converted from bare `LICENSE-MIT` / `LICENSE-APACHE` / `NOTICE` / `AUTHORS` targets to absolute GitHub URLs (`https://github.com/unclenate/auto-harness/blob/main/...`). Twenty directory-target (trailing-slash) markdown links across 10 profile/template/sample-project READMEs now point at the canonical file inside (`README.md`, `SKILL.md`, or `HARNESS.md`) instead of the directory itself. `platform/validators/README.md` Bash-version contradiction reworded (the 7 `validate-*.sh` scripts work on Bash 3.2+; only bootstrap scripts require Bash 4+). `Regexp.timeout = 1.0` set unconditionally in `platform/validators/lib/harness_registry.rb` to defend against ReDoS from user-supplied `companionRules.{triggerPaths,requiredAny,forbiddenPatterns}` and `.doc-reference-ignore` patterns. New top-level `CHANGELOG.md` (Keep-a-Changelog format) summarizing externally visible changes; cross-references the internal `docs/project/change-log.md` for per-decision audit trail. | The QUALITY-AUDIT-2026-05-18 lane-1 sweep checked filesystem existence of link targets but didn't catch *renderer* breakage: GitBook treats bare extensionless targets as directories and 404s on `<target>/README.md`. User reported a GitBook-published link to `LICENSE-MIT` resolving to a non-existent `LICENSE-MIT/README.md` (returning to `unclenate/auto-harness` via the redirected `unclenate/ai-prompts` old repo name). Same root cause as L1-14 (profile READMEs linking to skill directories). One coordinated PR closes the renderer-aware class for both. Bundled with L1-08 (validators/README Bash version, 1 line), L2-07 (`Regexp.timeout`, 1 line), and L1-09 (CHANGELOG.md, OSS hygiene) since each is a quick win from the audit's open list. | @unclenate | — |
| 2026-05-18 | Governance | Expanded `kernel/base` companion rule's `requiredAny` to accept `^docs/project/change-log\.md$` and `^docs/project/dependency-log\.md$` alongside the existing `ADR-` / `operating-principles.md` / `PRD-` satisfiers. Rewrote the rule's `humanReview` clause to distinguish substantive decisions (require ADR/PRD/operating-principles) from routine maintenance (may satisfy via change-log or dependency-log entry). Updated `docs/operating-principles.md § 2` to document the satisfier-scales-with-change-weight policy. | First Dependabot auto-PR (#17, `actions/checkout v4 → v6`) failed the kernel rule because Dependabot has no way to author ADR/PRD/operating-principles. Heavy-only satisfiers create perpetual friction for all routine dep bumps; bypassing via `--admin` trains the maintainer to ignore the validator. Cheaper satisfiers preserve auditability (rule still fires, every workflow edit still gates) while matching artifact weight to change weight. | @unclenate | ADR-0010 |
| 2026-05-18 | Technical | Catalog count reconciliation across entry-point docs: README.md (skills 5→7 incl. `harness-agentic-interfaces` + `harness-mcp` rows; validators 6→7 incl. `validate-doc-references.sh` row; starter-compositions table gains `interview-driven-discovery.yaml` and `mcp-server-typescript.yaml`; fixes `validate-companions.sh` invocation missing project-root arg; replaces `YOUR-ORG` with `unclenate`; adds macOS Bash 4 prerequisite paragraph). `platform/README.md` maturity claim aligned to Alpha (was "Beta (April 2026)"). `platform/reference/how-to-read.md` catalog counts updated (24/35/6/4 → 26/55/7/7) with invisible comment documenting count recipes; authority-stack diagram bumped (24→26 modules, 6→7 validators, 35→55 templates, 7→13 workflows). `platform/workflow/skills-and-agents.md` 5→7 with two new skill rows. `platform/workflow/submodule-integration.md` "five validators" → "seven validators" and two more `YOUR-ORG` → `unclenate`. Sample-project artifacts (submodule-consumer + node-web-saas-postgres) "six validators" → "seven". `docs/project/dependency-log.md` Ruby ≥ 2.7 → ≥ 3.0 (matches CONTRIBUTING.md and CI pin). | Catalog growth across PRs #5/#6/#9/#10 added two skills (`harness-agentic-interfaces`, `harness-mcp`), one validator (`validate-doc-references.sh`), and two compositions (`interview-driven-discovery.yaml`, `mcp-server-typescript.yaml`), but the entry-point docs never tracked the changes. The QUALITY-AUDIT-2026-05-18 audit (Triage item #1, findings L1-02/03/04/05/06/07/L4-01/04/05/13/02) consolidated 12 stale-count/broken-doc findings as one mechanical fold-back. Bundled as a single PR per audit's recommendation. | @unclenate | — |
| 2026-05-18 | Technical | Added `validate-doc-references.sh` (scans all platform/*.md for `platform/...` refs and asserts each resolves on disk; skips fenced code blocks; honors `.doc-reference-ignore`) and `forbiddenPatterns` companion-rule semantics (hard fail in `validate-companions.sh` when a changed-file path matches a forbidden regex, regardless of `requiredAny` satisfaction; forbidden-check runs first). Converted the codex-cli pack's documentary `AGENTS.override.md` ban into hard enforcement via the new field; bumped codex-cli to 0.2.0 and rewrote the README's "Mitigations" section to drop the "honest-but-documentary" language. CI workflow extended with a `Validate doc references` step. | The original validator chain had no static check for path drift across documentation; doc-reference bugs surfaced only during PR review. The codex-cli `AGENTS.override.md` rule was honest about its limitation (`requiredAny` could be satisfied by any documentary edit alongside the offending file). One coordinated branch closes both gaps by extending the existing companion-rule machinery rather than introducing a new validator script for the forbidden case. | @unclenate | — |
| 2026-05-18 | Scope | Promoted OPP-0002 to `accepted` with Disposition (accepted via ADR-0007, shipped in PR #5 commit 8aef150) and Promotion (ADR-0007 accepted same date, implementation-surface inventory, future PRDs deferred to consumer-driven signal) fields populated. Bumped ADR-0007 to Accepted with today's date. | The agentic-interfaces module set has been shipped and review-validated; the OPP and ADR records need to reflect the now-accepted state instead of remaining in proposed/staged form. | @unclenate | ADR-0007, OPP-0002 |
| 2026-05-18 | Scope | Promoted OPP-0003 to `accepted`, with Disposition and Promotion fields populated to match the now-accepted MCP-awareness direction and implementation state. | ADR-0008 resolved the architecture-vs-domain decision and scoped exposed governance via MCP as read-only/advisory v1; the opportunity record now reflects that accepted outcome instead of remaining in proposed state. | @unclenate | ADR-0008, OPP-0003 |
| 2026-05-17 | Discovery | Filed OPP-0002 (Agentic Interface Awareness) and proposed via ADR-0007. Adds `domains/agentic-interfaces` (cross-cutting module covering design doc, risk register, prompt-tool registry, renderer contract for in-product agent surfaces) and `architectures/agentic-ui` (optional topology overlay for conversational-primary, MCP-host, and Open-ended generative-UI products). Ships template family, `harness-agentic-interfaces` Agent Skill, integration workflow guide, `agentic-ui-saas.yaml` starter composition, and `agentic-ui-starter` sample project. Vendor-neutral at the module level (Controlled / Declarative / Open-ended / Conversational-primary flavor map); CopilotKit, A2UI v0.8, MCP Apps cited as canonical implementations with detail carried in templates and the skill. | The "agentic interface" pattern (CopilotKit-style copilots, A2UI-style generative UI, MCP Apps, conversational-primary products) consolidated across the React/Next.js ecosystem in 2026. Auto-harness had no module that named the shape or governed its risks (prompt-injection via tool results, hallucinated UI affordances, action-approval bypass, generative-UI XSS, renderer-contract drift, model-update regression). Closes the gap on the consumer-side, paralleling OPP-0001 on the runtime-harness side. R&D pass — committed on `feature/agentic-interfaces-rnd` for maintainer review before merge. | @unclenate | OPP-0002, ADR-0007 |
| 2026-05-15 | Scope | Open-source cut: dual MIT/Apache-2.0 licensing, SPDX/copyright headers across all source files via deterministic `platform/bootstrap/add-license-headers.sh` script, new `Maintenance & Operations` workflow section consolidating previously-scattered post-adoption content (upgrade flow, version pinning, drift recovery, lifecycle transitions, periodic audits), TOC reorganization in README.md and SUMMARY.md around an Adoption → Day-to-Day → Maintenance → Reference axis, full community-file foundation (`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` adopting Contributor Covenant v2.1 by reference, `SECURITY.md` with harness-specific threat model, `AUTHORS`, `NOTICE`), GitHub issue forms (bug / feature / observation — the last channeling external contributors into the knowledge-capture flow) and a PR template embedding companion-rule discipline. Attribution standardized to `UncleNate@gmail.com` (personal email for personal IP), distinct from any commercial-project email. | Pre-launch readiness audit surfaced two coupled issues: information architecture conflated adoption, day-to-day, and post-adoption maintenance under a flat "Workflow Guides" heading; and the repository had no legal foundation (no LICENSE, no per-file indicia, no community files). Single coordinated PR addresses both so the public flip is one clean cut. Dual-license pattern (Rust-ecosystem convention) maximizes downstream optionality without imposing NOTICE-only friction. | @unclenate | ADR-0005 |
| 2026-05-12 | Technical | Documentation alignment pass: HARNESS.md Active Modules table, SUMMARY.md (Module Library + Templates sections), README.md directory tree, `platform/skills/harness-onboarding/SKILL.md` management catalog, `platform/templates/README.md` Opportunity section, and `platform/workflow/discovery-to-composition.md` decision rubric all updated to reflect `management/knowledge-capture`, `management/opportunity-capture`, and `agents/openclaw` (the latter two of which had been added without back-propagating to catalog docs). `docs/operating-principles.md` refreshed (Last updated bumped; added explicit catalog-propagation principle to § 3). | Active module catalog had drifted from the canonical docs — `knowledge-capture` and `openclaw` were activated but never propagated; `opportunity-capture` was just added. Section 3 of operating principles explicitly requires catalog updates in the same pass, so this aligns the operating principle with the practice. | @unclenate | — |
| 2026-05-12 | Discovery | Filed OPP-0001 (Exportable Governance Contract for Runtime Harnesses) as the first candidate under the new opportunity-capture module. Paired with a structured observation in `docs/knowledge/shared-observations.md` documenting the runtime-harness vs. governance-harness genre distinction surfaced by reviewing adenhq/hive. Status: proposed; Confidence: medium. | First end-to-end exercise of the opportunity-capture module: the candidate cites the backward-looking observation as its Origin / Evidence, closing the gap between forward-looking ideas and backward-looking observations at the explicit evidence link rather than by merging the two surfaces. | @unclenate | OPP-0001 |
| 2026-05-12 | Scope | Added `management/opportunity-capture` module: new opt-in surface (`docs/opportunities/`) for pre-PRD candidate records with status (proposed / exploring / accepted / declined / superseded), evidence linkage to `docs/knowledge/shared-observations.md`, and a binding promotion contract (status=accepted requires PRD in same commit). Two companion rules wired via existing validators; no new validator code. auto-harness adopts the module in its own manifest. | A session-level analysis surfaced the gap between forward-looking ideas and backward-looking observations: the harness had no native surface for pre-PRD candidates, so product-shaped insights died in conversation history. Closes the gap at the explicit evidence link rather than by merging surfaces. | @unclenate | PRD-0003, ADR-0004 |
| 2026-05-09 | Scope | Extended PRD template with optional execution-spec sections (Goals & Non-Goals, Tech Stack, API & Data, UI/UX Notes, CI/CD Gates) | A talk on hackathon-style PRDs surfaced gaps where the template, optimized as a governance record, doesn't constrain AI-agent build choices. Single template with "when applicable" sections supports both flavors; PRD-0001 backfilled to demonstrate `N/A — governance PRD` convention; companion rules unchanged. | @unclenate | PRD-0002 |
| 2026-04-21 | Technical | Adopted git submodule as first-class consumption model with brownfield-safe bootstrap and multi-platform coexistence | Copy-in adoption caused upstream drift; consumers rarely re-synced; multi-platform repos needed explicit coexistence guarantees. Shipped: `install.sh` + `link-skills.sh` bootstrap tools, `submodule-integration.md` guide, `TestSubmoduleMount` proof, doc updates across `ci-integration.md` / `bootstrap-quickstart.md` / `brownfield-onboarding.md` / `SKILL.md` / `README.md`. | @unclenate | ADR-0003 |
| 2026-04-20 | Technical | Added CI self-governance workflow and installed `harness-governance` skill for self-application | Harness now enforces its own validator chain and self-tests on every PR/push to main, and agents working on the harness operate under the governance skill it provides to projects | @unclenate | — |
| 2026-04-08 | Technical | Bootstrapped `AGENTS.md` as the workspace-instructions entrypoint | Keep one cross-tool source of truth for agent guidance and avoid duplicated Copilot-specific instructions | @unclenate | — |
| 2026-04-07 | Scope | Restored PRDs as first-class record type | PRD process was lost in monolith-to-modular decomposition; product decisions had no structured rationale record | @unclenate | PRD-0001 |
| 2026-04-07 | Scope | Added self-governance artifacts | Harness should eat its own dog food; declared modules but had no artifacts | @unclenate | ADR-0001 |
| 2026-04-07 | Scope | Added product-lite module to manifest | Required for PRD companion rules and product artifact tracking | @unclenate | — |
| 2026-04-07 | Technical | Repo renamed from ai-prompts to auto-harness | Name better reflects the project's purpose as a governance framework | @unclenate | — |
| 2026-04-07 | Technical | Promoted development-harness/ to repo root | Eliminated unnecessary nesting; simplified all internal path references | @unclenate | — |

---

## What Belongs Here

Add an entry when:

- A requirement is added, removed, or significantly changed
- A milestone is moved or dropped
- An architectural decision changes direction
- A feature is explicitly deferred to a future release
- A third-party dependency changes

Do NOT add entries for routine code changes, minor doc fixes, or bug fixes that don't
change scope or direction.

---

## Reference

| Resource | Path |
| -------- | ---- |
| Requirements | `docs/product/requirements.md` |
| ADR directory | `docs/adr/` |
| PRD directory | `docs/requirements/` |
| Milestones | `docs/project/milestones.md` |
